# gravity.electron

> charge is made in pairs and destroyed in pairs, what is left over is bent by everything inside it, and the bending itself weighs. What does a bound cloud come to, and how does it stand against the tightest thing that can close?

Proved under `G`, `G^XOR`, `G^XOR*2`, `G^XOR^c`, `G^XOR+XOR`, `G^XOR^o`, `G^XOR · relaxing(2/1)`. Generated by `implementations/.ts/src/theorems` - nothing here was written by hand: every premise is a count or an exact invariance taken from the theory itself, and the conclusion is what the inference rules made of them. [Open the page](index.html) to switch theory and lattice.

| theory | answer |
|---|---|
| `G` | \frac{M}{M_0} = e^(f·r) - 1 |
| `G^XOR` | \frac{M}{M_0} = e^(f·r) - 1 |
| `G^XOR*2` | \frac{M}{M_0} = e^(f·r) - 1 |
| `G^XOR^c` | \frac{M}{M_0} = e^(f·r) - 1 |
| `G^XOR+XOR` | \frac{M}{M_0} = e^(f·r) - 1 |
| `G^XOR^o` | \frac{M}{M_0} = e^(f·r) - 1 |
| `G^XOR · relaxing(2/1)` | \frac{M}{M_0} = e^(f·r) - 1 |

## G

### the result

**\frac{M}{M_0} = e^(f·r) - 1**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**\frac{dM}{dr} = \frac{dM}{dr}**  
<sub>definition · gravity.electron</sub>  

and that is what dM/dr MEANS - the rate the enclosed mass grows at as the radius does. Naming it is not a premise about the world; it is saying which derivative the product above is

**\frac{dM}{dr} = shell · \omega · f**  
<sub>definition · gravity.electron</sub>  

AND WHAT THE NEXT SHELL ADDS IS ITS CELLS, TIMES WHAT EACH IS TURNING AT, TIMES WHAT SURVIVES THE CANCELLING. (G/2) makes charge in pairs and ANNIHILATION unmakes it in pairs, so what can act on anything is the residual, and f is that fraction - a property of the meeting rule, measured rather than assumed. THE THREE FACTORS ARE HANDED OVER SEPARATELY AND NOT MULTIPLIED HERE: what they come to is for the rules to work out, and the cancellation between the room and the falloff is the whole content of this theorem rather than something to be asserted in a sentence

**\omega = \frac{M}{STEP·r^(D-1)}**  
<sub>definition · gravity.electron</sub>  

WHAT BENDS A CELL IS EVERYTHING INSIDE IT. The second premise says turning emits gravity and `lib/Trajectory.ts` says mass IS turning, so the cloud that has already gathered is part of what does the bending - the field reads M and not the source alone. It spreads over the room above, which is the only geometry in this

**\frac{dM}{dr} ∝ \frac{M·f·shell}{r^(D-1)}**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**\frac{M}{M_0} = e^(f·r) - 1**  
<sub>derived · a rate proportional to itself</sub>  

and what has ACCUMULATED is that less what it started from, so the ratio of the two is set by the single dimensionless product f·r̄ and by nothing else at all - not by the dimension, not by the lattice, not by how far anything reaches on its own

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. fcc-12: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. cubic-6: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. square-4: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. line-2: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

## G^XOR

### the result

**\frac{M}{M_0} = e^(f·r) - 1**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**\frac{dM}{dr} = \frac{dM}{dr}**  
<sub>definition · gravity.electron</sub>  

and that is what dM/dr MEANS - the rate the enclosed mass grows at as the radius does. Naming it is not a premise about the world; it is saying which derivative the product above is

**\frac{dM}{dr} = shell · \omega · f**  
<sub>definition · gravity.electron</sub>  

AND WHAT THE NEXT SHELL ADDS IS ITS CELLS, TIMES WHAT EACH IS TURNING AT, TIMES WHAT SURVIVES THE CANCELLING. (G/2) makes charge in pairs and ANNIHILATION unmakes it in pairs, so what can act on anything is the residual, and f is that fraction - a property of the meeting rule, measured rather than assumed. THE THREE FACTORS ARE HANDED OVER SEPARATELY AND NOT MULTIPLIED HERE: what they come to is for the rules to work out, and the cancellation between the room and the falloff is the whole content of this theorem rather than something to be asserted in a sentence

**\omega = \frac{M}{STEP·r^(D-1)}**  
<sub>definition · gravity.electron</sub>  

WHAT BENDS A CELL IS EVERYTHING INSIDE IT. The second premise says turning emits gravity and `lib/Trajectory.ts` says mass IS turning, so the cloud that has already gathered is part of what does the bending - the field reads M and not the source alone. It spreads over the room above, which is the only geometry in this

**\frac{dM}{dr} ∝ \frac{M·f·shell}{r^(D-1)}**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**\frac{M}{M_0} = e^(f·r) - 1**  
<sub>derived · a rate proportional to itself</sub>  

and what has ACCUMULATED is that less what it started from, so the ratio of the two is set by the single dimensionless product f·r̄ and by nothing else at all - not by the dimension, not by the lattice, not by how far anything reaches on its own

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. fcc-12: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. cubic-6: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. square-4: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. line-2: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

## G^XOR*2

### the result

**\frac{M}{M_0} = e^(f·r) - 1**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**\frac{dM}{dr} = \frac{dM}{dr}**  
<sub>definition · gravity.electron</sub>  

and that is what dM/dr MEANS - the rate the enclosed mass grows at as the radius does. Naming it is not a premise about the world; it is saying which derivative the product above is

**\frac{dM}{dr} = shell · \omega · f**  
<sub>definition · gravity.electron</sub>  

AND WHAT THE NEXT SHELL ADDS IS ITS CELLS, TIMES WHAT EACH IS TURNING AT, TIMES WHAT SURVIVES THE CANCELLING. (G/2) makes charge in pairs and ANNIHILATION unmakes it in pairs, so what can act on anything is the residual, and f is that fraction - a property of the meeting rule, measured rather than assumed. THE THREE FACTORS ARE HANDED OVER SEPARATELY AND NOT MULTIPLIED HERE: what they come to is for the rules to work out, and the cancellation between the room and the falloff is the whole content of this theorem rather than something to be asserted in a sentence

**\omega = \frac{M}{STEP·r^(D-1)}**  
<sub>definition · gravity.electron</sub>  

WHAT BENDS A CELL IS EVERYTHING INSIDE IT. The second premise says turning emits gravity and `lib/Trajectory.ts` says mass IS turning, so the cloud that has already gathered is part of what does the bending - the field reads M and not the source alone. It spreads over the room above, which is the only geometry in this

**\frac{dM}{dr} ∝ \frac{M·f·shell}{r^(D-1)}**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**\frac{M}{M_0} = e^(f·r) - 1**  
<sub>derived · a rate proportional to itself</sub>  

and what has ACCUMULATED is that less what it started from, so the ratio of the two is set by the single dimensionless product f·r̄ and by nothing else at all - not by the dimension, not by the lattice, not by how far anything reaches on its own

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. fcc-12: the cancelling leaves 8.6% of the charge standing (+/- 0.4), measured as the RMS at one moment

- f = 0.0855 +/- 0.0044 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. cubic-6: the cancelling leaves 8.7% of the charge standing (+/- 0.2), measured as the RMS at one moment

- f = 0.0866 +/- 0.0019 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. square-4: the cancelling leaves 16.4% of the charge standing (+/- 0.7), measured as the RMS at one moment

- f = 0.1636 +/- 0.0067 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. line-2: the cancelling leaves 35.6% of the charge standing (+/- 1.2), measured as the RMS at one moment

- f = 0.3562 +/- 0.0121 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

## G^XOR^c

### the result

**\frac{M}{M_0} = e^(f·r) - 1**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**\frac{dM}{dr} = \frac{dM}{dr}**  
<sub>definition · gravity.electron</sub>  

and that is what dM/dr MEANS - the rate the enclosed mass grows at as the radius does. Naming it is not a premise about the world; it is saying which derivative the product above is

**\frac{dM}{dr} = shell · \omega · f**  
<sub>definition · gravity.electron</sub>  

AND WHAT THE NEXT SHELL ADDS IS ITS CELLS, TIMES WHAT EACH IS TURNING AT, TIMES WHAT SURVIVES THE CANCELLING. (G/2) makes charge in pairs and ANNIHILATION unmakes it in pairs, so what can act on anything is the residual, and f is that fraction - a property of the meeting rule, measured rather than assumed. THE THREE FACTORS ARE HANDED OVER SEPARATELY AND NOT MULTIPLIED HERE: what they come to is for the rules to work out, and the cancellation between the room and the falloff is the whole content of this theorem rather than something to be asserted in a sentence

**\omega = \frac{M}{STEP·r^(D-1)}**  
<sub>definition · gravity.electron</sub>  

WHAT BENDS A CELL IS EVERYTHING INSIDE IT. The second premise says turning emits gravity and `lib/Trajectory.ts` says mass IS turning, so the cloud that has already gathered is part of what does the bending - the field reads M and not the source alone. It spreads over the room above, which is the only geometry in this

**\frac{dM}{dr} ∝ \frac{M·f·shell}{r^(D-1)}**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**\frac{M}{M_0} = e^(f·r) - 1**  
<sub>derived · a rate proportional to itself</sub>  

and what has ACCUMULATED is that less what it started from, so the ratio of the two is set by the single dimensionless product f·r̄ and by nothing else at all - not by the dimension, not by the lattice, not by how far anything reaches on its own

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. fcc-12: the cancelling leaves 9.7% of the charge standing (+/- 0.5), measured as the RMS at one moment

- f = 0.0967 +/- 0.0048 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. cubic-6: the cancelling leaves 7.4% of the charge standing (+/- 0.2), measured as the RMS at one moment

- f = 0.0742 +/- 0.0019 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. square-4: the cancelling leaves 17.4% of the charge standing (+/- 1.5), measured as the RMS at one moment

- f = 0.1744 +/- 0.0150 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. line-2: the cancelling leaves 41.6% of the charge standing (+/- 2.6), measured as the RMS at one moment

- f = 0.4160 +/- 0.0256 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

## G^XOR+XOR

### the result

**\frac{M}{M_0} = e^(f·r) - 1**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**\frac{dM}{dr} = \frac{dM}{dr}**  
<sub>definition · gravity.electron</sub>  

and that is what dM/dr MEANS - the rate the enclosed mass grows at as the radius does. Naming it is not a premise about the world; it is saying which derivative the product above is

**\frac{dM}{dr} = shell · \omega · f**  
<sub>definition · gravity.electron</sub>  

AND WHAT THE NEXT SHELL ADDS IS ITS CELLS, TIMES WHAT EACH IS TURNING AT, TIMES WHAT SURVIVES THE CANCELLING. (G/2) makes charge in pairs and ANNIHILATION unmakes it in pairs, so what can act on anything is the residual, and f is that fraction - a property of the meeting rule, measured rather than assumed. THE THREE FACTORS ARE HANDED OVER SEPARATELY AND NOT MULTIPLIED HERE: what they come to is for the rules to work out, and the cancellation between the room and the falloff is the whole content of this theorem rather than something to be asserted in a sentence

**\omega = \frac{M}{STEP·r^(D-1)}**  
<sub>definition · gravity.electron</sub>  

WHAT BENDS A CELL IS EVERYTHING INSIDE IT. The second premise says turning emits gravity and `lib/Trajectory.ts` says mass IS turning, so the cloud that has already gathered is part of what does the bending - the field reads M and not the source alone. It spreads over the room above, which is the only geometry in this

**\frac{dM}{dr} ∝ \frac{M·f·shell}{r^(D-1)}**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**\frac{M}{M_0} = e^(f·r) - 1**  
<sub>derived · a rate proportional to itself</sub>  

and what has ACCUMULATED is that less what it started from, so the ratio of the two is set by the single dimensionless product f·r̄ and by nothing else at all - not by the dimension, not by the lattice, not by how far anything reaches on its own

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. fcc-12: the cancelling leaves 8.6% of the charge standing (+/- 0.4), measured as the RMS at one moment

- f = 0.0855 +/- 0.0044 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. cubic-6: the cancelling leaves 8.7% of the charge standing (+/- 0.2), measured as the RMS at one moment

- f = 0.0866 +/- 0.0019 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. square-4: the cancelling leaves 16.4% of the charge standing (+/- 0.7), measured as the RMS at one moment

- f = 0.1636 +/- 0.0067 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. line-2: the cancelling leaves 35.6% of the charge standing (+/- 1.2), measured as the RMS at one moment

- f = 0.3562 +/- 0.0121 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

## G^XOR^o

### the result

**\frac{M}{M_0} = e^(f·r) - 1**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**\frac{dM}{dr} = \frac{dM}{dr}**  
<sub>definition · gravity.electron</sub>  

and that is what dM/dr MEANS - the rate the enclosed mass grows at as the radius does. Naming it is not a premise about the world; it is saying which derivative the product above is

**\frac{dM}{dr} = shell · \omega · f**  
<sub>definition · gravity.electron</sub>  

AND WHAT THE NEXT SHELL ADDS IS ITS CELLS, TIMES WHAT EACH IS TURNING AT, TIMES WHAT SURVIVES THE CANCELLING. (G/2) makes charge in pairs and ANNIHILATION unmakes it in pairs, so what can act on anything is the residual, and f is that fraction - a property of the meeting rule, measured rather than assumed. THE THREE FACTORS ARE HANDED OVER SEPARATELY AND NOT MULTIPLIED HERE: what they come to is for the rules to work out, and the cancellation between the room and the falloff is the whole content of this theorem rather than something to be asserted in a sentence

**\omega = \frac{M}{STEP·r^(D-1)}**  
<sub>definition · gravity.electron</sub>  

WHAT BENDS A CELL IS EVERYTHING INSIDE IT. The second premise says turning emits gravity and `lib/Trajectory.ts` says mass IS turning, so the cloud that has already gathered is part of what does the bending - the field reads M and not the source alone. It spreads over the room above, which is the only geometry in this

**\frac{dM}{dr} ∝ \frac{M·f·shell}{r^(D-1)}**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**\frac{M}{M_0} = e^(f·r) - 1**  
<sub>derived · a rate proportional to itself</sub>  

and what has ACCUMULATED is that less what it started from, so the ratio of the two is set by the single dimensionless product f·r̄ and by nothing else at all - not by the dimension, not by the lattice, not by how far anything reaches on its own

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. fcc-12: the cancelling leaves 8.6% of the charge standing (+/- 0.4), measured as the RMS at one moment

- f = 0.0855 +/- 0.0044 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. cubic-6: the cancelling leaves 8.7% of the charge standing (+/- 0.2), measured as the RMS at one moment

- f = 0.0866 +/- 0.0019 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. square-4: the cancelling leaves 16.4% of the charge standing (+/- 0.7), measured as the RMS at one moment

- f = 0.1636 +/- 0.0067 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - holds. line-2: the cancelling leaves 35.6% of the charge standing (+/- 1.2), measured as the RMS at one moment

- f = 0.3562 +/- 0.0121 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

## G^XOR · relaxing(2/1)

### the result

**\frac{M}{M_0} = e^(f·r) - 1**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**\frac{dM}{dr} = \frac{dM}{dr}**  
<sub>definition · gravity.electron</sub>  

and that is what dM/dr MEANS - the rate the enclosed mass grows at as the radius does. Naming it is not a premise about the world; it is saying which derivative the product above is

**\frac{dM}{dr} = shell · \omega · f**  
<sub>definition · gravity.electron</sub>  

AND WHAT THE NEXT SHELL ADDS IS ITS CELLS, TIMES WHAT EACH IS TURNING AT, TIMES WHAT SURVIVES THE CANCELLING. (G/2) makes charge in pairs and ANNIHILATION unmakes it in pairs, so what can act on anything is the residual, and f is that fraction - a property of the meeting rule, measured rather than assumed. THE THREE FACTORS ARE HANDED OVER SEPARATELY AND NOT MULTIPLIED HERE: what they come to is for the rules to work out, and the cancellation between the room and the falloff is the whole content of this theorem rather than something to be asserted in a sentence

**\omega = \frac{M}{STEP·r^(D-1)}**  
<sub>definition · gravity.electron</sub>  

WHAT BENDS A CELL IS EVERYTHING INSIDE IT. The second premise says turning emits gravity and `lib/Trajectory.ts` says mass IS turning, so the cloud that has already gathered is part of what does the bending - the field reads M and not the source alone. It spreads over the room above, which is the only geometry in this

**\frac{dM}{dr} ∝ \frac{M·f·shell}{r^(D-1)}**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**\frac{M}{M_0} = e^(f·r) - 1**  
<sub>derived · a rate proportional to itself</sub>  

and what has ACCUMULATED is that less what it started from, so the ratio of the two is set by the single dimensionless product f·r̄ and by nothing else at all - not by the dimension, not by the lattice, not by how far anything reaches on its own

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. fcc-12: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. cubic-6: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. square-4: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`residual/what-the-cancelling-leaves` - does not hold. line-2: this theory puts no sign on what it carries, so there is nothing to cancel and no residual to measure

- f = 0 - the net charge in a ball of 4 cells at one moment, over the gross charge there - the RMS across 60 ticks and 3 seeds, not the mean, which in a vacuum with no preferred sign measures the sampling instead of the vacuum
