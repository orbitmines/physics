# lattice.phase

> the exits about an axis form a ring. How far round is one step of it?

Proved under `G`, `G^XOR`, `G^XOR*2`, `G^XOR · relaxing(2/1)`. Generated by `implementations/.ts/src/theorems` - nothing here was written by hand: every premise is a count or an exact invariance taken from the theory itself, and the conclusion is what the inference rules made of them. [Open the page](index.html) to switch theory and lattice.

| theory | answer |
|---|---|
| `G` | SPIN = 1/2·\pi (and 1 other result) |
| `G^XOR` | SPIN = 1/2·\pi (and 1 other result) |
| `G^XOR*2` | SPIN = 1/2·\pi (and 1 other result) |
| `G^XOR · relaxing(2/1)` | SPIN = 1/2·\pi (and 1 other result) |

## G

### result 1 of 2

**SPIN = 1/2·\pi**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |

#### the derivation

**SPIN = \frac{2\pi}{turn order}**  
<sub>premise · turning/is-a-turn-a-rotation</sub>  

one turn is one step of a ring of 4, so it is a whole turn over 4 - the phase quantum, and a count of the tiling rather than an angle anybody chose

**turn order = 4**  
<sub>premise · turning/is-a-turn-a-rotation</sub>  

the theory's own turn was applied to a direction until it came home, which took 4 applications: 0 -> 8 -> 11 -> 3 -> 0. That count is the ring's length. It is also the proof that a turn is a ROTATION rather than a reflection, since a reflection has order two - and everything magnetic in this model rests on that distinction

**turn order = 4**  
<sub>derived · a number is an expression</sub>  

turn order is 4, so it can stand in an expression as that

**SPIN = 1/2·\pi**  
<sub>derived · substituting</sub>  

```
SPIN = 2·\frac{\pi}{turn order}
turn order = 4
SPIN = 1/2·\pi
```

turn order is not a primitive of this theory - it is 4, so it stands in for itself here and the result is multiplied out

#### what the runs found

**`fcc-12`**

`turning/is-a-turn-a-rotation` - holds. a turn on fcc-12 has order 4 - a ROTATION, not a reflection - about an axis the rule is genuinely given, so its antisymmetric part is a cross product and what it leaves transverse to a motion lies along v x b-hat. The coupling is the exits' second moment, 4.0000 = DEG/D, isotropic to 7.8e-16

- what an alike meeting does to a direction = 0 - ANNIHILATION left the direction alone entirely
- turns that bring a direction home = 4 - applying G's own turn about an axis, exit 0 goes 0 -> 8 -> 11 -> 3 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 1 - turning exit 0 about [0,0,1] gives 8 and about [1,0,0] gives 1 - different, so the rule sweeps the axis it was given
- \Sigma \hat{d} \otimes \hat{d} = 4.0000 - the exits' second moment on the unit vectors, summed over all 12 of them: 4.000000 against DEG/D = 4.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 6 - 6, against the 4 walked about this axis - DIFFERENT, and both are right. The geometry's CYCLE is the length of its default ring; the order here is the ring about the axis this turn was handed. That a turn's order depends on which axis it is about is a fact about fcc-12 worth having, and it is the reason this quantity is not called CYCLE

**`cubic-6`**

`turning/is-a-turn-a-rotation` - holds. a turn on cubic-6 has order 4 - a ROTATION, not a reflection - about an axis the rule is genuinely given, so its antisymmetric part is a cross product and what it leaves transverse to a motion lies along v x b-hat. The coupling is the exits' second moment, 2.0000 = DEG/D, isotropic to 5.6e-16

- what an alike meeting does to a direction = 0 - ANNIHILATION left the direction alone entirely
- turns that bring a direction home = 4 - applying G's own turn about an axis, exit 0 goes 0 -> 1 -> 5 -> 4 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 1 - turning exit 0 about [0,0,1] gives 1 and about [1,0,0] gives 0 - different, so the rule sweeps the axis it was given
- \Sigma \hat{d} \otimes \hat{d} = 2 - the exits' second moment on the unit vectors, summed over all 6 of them: 2.000000 against DEG/D = 2.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 4 - the same as the order walked here, so the default ring and the ring about this axis are one ring

**`square-4`**

`turning/is-a-turn-a-rotation` - holds. a turn on square-4 has order 4 - a ROTATION, not a reflection - about an axis the rule is genuinely given, so its antisymmetric part is a cross product and what it leaves transverse to a motion lies along v x b-hat. The coupling is the exits' second moment, 2.0000 = DEG/D, isotropic to 4.4e-16

- what an alike meeting does to a direction = 0 - ANNIHILATION left the direction alone entirely
- turns that bring a direction home = 4 - applying G's own turn about an axis, exit 0 goes 0 -> 1 -> 3 -> 2 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 0 - both axes give 1, so the rule is not using the axis it was handed and there is no free direction in it for a field to lie along
- \Sigma \hat{d} \otimes \hat{d} = 2 - the exits' second moment on the unit vectors, summed over all 4 of them: 2.000000 against DEG/D = 2.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 4 - the same as the order walked here, so the default ring and the ring about this axis are one ring

### result 2 of 2

**no law follows for SPIN about \hat{b}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- turn order = 0

#### the derivation

There isn't one - the rules could reach no law from what the probes found.

#### what the runs found

`turning/is-a-turn-a-rotation` - does not hold. a turn is a turn in a PLANE and line-2 has 1 dimension, so there is no plane to turn in and nothing here can be a rotation


## G^XOR

### result 1 of 2

**SPIN = 1/2·\pi**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |

#### the derivation

**SPIN = \frac{2\pi}{turn order}**  
<sub>premise · turning/is-a-turn-a-rotation</sub>  

one turn is one step of a ring of 4, so it is a whole turn over 4 - the phase quantum, and a count of the tiling rather than an angle anybody chose

**turn order = 4**  
<sub>premise · turning/is-a-turn-a-rotation</sub>  

the theory's own turn was applied to a direction until it came home, which took 4 applications: 0 -> 8 -> 11 -> 3 -> 0. That count is the ring's length. It is also the proof that a turn is a ROTATION rather than a reflection, since a reflection has order two - and everything magnetic in this model rests on that distinction

**turn order = 4**  
<sub>derived · a number is an expression</sub>  

turn order is 4, so it can stand in an expression as that

**SPIN = 1/2·\pi**  
<sub>derived · substituting</sub>  

```
SPIN = 2·\frac{\pi}{turn order}
turn order = 4
SPIN = 1/2·\pi
```

turn order is not a primitive of this theory - it is 4, so it stands in for itself here and the result is multiplied out

#### what the runs found

**`fcc-12`**

`turning/is-a-turn-a-rotation` - holds. G^XOR's alike meeting REVERSES a direction - ANNIHILATION marks it bounced and a bounce resolves through the opposite exit, which is a reflection of order two. A reflection's matrix is symmetric and has no antisymmetric part, so there is no cross product in the dynamics and no perpendicular force: not a small one, none. The lattice DOES carry a ring - a direction comes home after 4 turns about a given axis, and the exits' second moment is 4.0000 = DEG/D, isotropic to 7.8e-16 - which is everything the rotation route would be made of. No rule here consults it. That is why a magnetic force in this implementation needs a direction carried on the ray by hand, which is what G^LABELLED adds and why it exists

- what an alike meeting does to a direction = 2 - ANNIHILATION marks both ends bounced, and a bounce is resolved through OPP - the opposite exit. Going back the way you came is a REVERSAL: order two, which is a reflection. A reflection has no antisymmetric part, so there is no cross product in it and no perpendicular force
- turns that bring a direction home = 4 - applying G^XOR's own turn about an axis, exit 0 goes 0 -> 8 -> 11 -> 3 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 1 - turning exit 0 about [0,0,1] gives 8 and about [1,0,0] gives 1 - different, so the rule sweeps the axis it was given
- \Sigma \hat{d} \otimes \hat{d} = 4.0000 - the exits' second moment on the unit vectors, summed over all 12 of them: 4.000000 against DEG/D = 4.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 6 - 6, against the 4 walked about this axis - DIFFERENT, and both are right. The geometry's CYCLE is the length of its default ring; the order here is the ring about the axis this turn was handed. That a turn's order depends on which axis it is about is a fact about fcc-12 worth having, and it is the reason this quantity is not called CYCLE

**`cubic-6`**

`turning/is-a-turn-a-rotation` - holds. G^XOR's alike meeting REVERSES a direction - ANNIHILATION marks it bounced and a bounce resolves through the opposite exit, which is a reflection of order two. A reflection's matrix is symmetric and has no antisymmetric part, so there is no cross product in the dynamics and no perpendicular force: not a small one, none. The lattice DOES carry a ring - a direction comes home after 4 turns about a given axis, and the exits' second moment is 2.0000 = DEG/D, isotropic to 5.6e-16 - which is everything the rotation route would be made of. No rule here consults it. That is why a magnetic force in this implementation needs a direction carried on the ray by hand, which is what G^LABELLED adds and why it exists

- what an alike meeting does to a direction = 2 - ANNIHILATION marks both ends bounced, and a bounce is resolved through OPP - the opposite exit. Going back the way you came is a REVERSAL: order two, which is a reflection. A reflection has no antisymmetric part, so there is no cross product in it and no perpendicular force
- turns that bring a direction home = 4 - applying G^XOR's own turn about an axis, exit 0 goes 0 -> 1 -> 5 -> 4 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 1 - turning exit 0 about [0,0,1] gives 1 and about [1,0,0] gives 0 - different, so the rule sweeps the axis it was given
- \Sigma \hat{d} \otimes \hat{d} = 2 - the exits' second moment on the unit vectors, summed over all 6 of them: 2.000000 against DEG/D = 2.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 4 - the same as the order walked here, so the default ring and the ring about this axis are one ring

**`square-4`**

`turning/is-a-turn-a-rotation` - holds. G^XOR's alike meeting REVERSES a direction - ANNIHILATION marks it bounced and a bounce resolves through the opposite exit, which is a reflection of order two. A reflection's matrix is symmetric and has no antisymmetric part, so there is no cross product in the dynamics and no perpendicular force: not a small one, none. The lattice DOES carry a ring - a direction comes home after 4 turns about a given axis, and the exits' second moment is 2.0000 = DEG/D, isotropic to 4.4e-16 - which is everything the rotation route would be made of. No rule here consults it. That is why a magnetic force in this implementation needs a direction carried on the ray by hand, which is what G^LABELLED adds and why it exists

- what an alike meeting does to a direction = 2 - ANNIHILATION marks both ends bounced, and a bounce is resolved through OPP - the opposite exit. Going back the way you came is a REVERSAL: order two, which is a reflection. A reflection has no antisymmetric part, so there is no cross product in it and no perpendicular force
- turns that bring a direction home = 4 - applying G^XOR's own turn about an axis, exit 0 goes 0 -> 1 -> 3 -> 2 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 0 - both axes give 1, so the rule is not using the axis it was handed and there is no free direction in it for a field to lie along
- \Sigma \hat{d} \otimes \hat{d} = 2 - the exits' second moment on the unit vectors, summed over all 4 of them: 2.000000 against DEG/D = 2.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 4 - the same as the order walked here, so the default ring and the ring about this axis are one ring

### result 2 of 2

**no law follows for SPIN about \hat{b}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- turn order = 0

#### the derivation

There isn't one - the rules could reach no law from what the probes found.

#### what the runs found

`turning/is-a-turn-a-rotation` - does not hold. a turn is a turn in a PLANE and line-2 has 1 dimension, so there is no plane to turn in and nothing here can be a rotation


## G^XOR*2

### result 1 of 2

**SPIN = 1/2·\pi**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |

#### the derivation

**SPIN = \frac{2\pi}{turn order}**  
<sub>premise · turning/is-a-turn-a-rotation</sub>  

one turn is one step of a ring of 4, so it is a whole turn over 4 - the phase quantum, and a count of the tiling rather than an angle anybody chose

**turn order = 4**  
<sub>premise · turning/is-a-turn-a-rotation</sub>  

the theory's own turn was applied to a direction until it came home, which took 4 applications: 0 -> 8 -> 11 -> 3 -> 0. That count is the ring's length. It is also the proof that a turn is a ROTATION rather than a reflection, since a reflection has order two - and everything magnetic in this model rests on that distinction

**turn order = 4**  
<sub>derived · a number is an expression</sub>  

turn order is 4, so it can stand in an expression as that

**SPIN = 1/2·\pi**  
<sub>derived · substituting</sub>  

```
SPIN = 2·\frac{\pi}{turn order}
turn order = 4
SPIN = 1/2·\pi
```

turn order is not a primitive of this theory - it is 4, so it stands in for itself here and the result is multiplied out

#### what the runs found

**`fcc-12`**

`turning/is-a-turn-a-rotation` - holds. G^XOR*2's alike meeting REVERSES a direction - ANNIHILATION marks it bounced and a bounce resolves through the opposite exit, which is a reflection of order two. A reflection's matrix is symmetric and has no antisymmetric part, so there is no cross product in the dynamics and no perpendicular force: not a small one, none. The lattice DOES carry a ring - a direction comes home after 4 turns about a given axis, and the exits' second moment is 4.0000 = DEG/D, isotropic to 7.8e-16 - which is everything the rotation route would be made of. No rule here consults it. That is why a magnetic force in this implementation needs a direction carried on the ray by hand, which is what G^LABELLED adds and why it exists

- what an alike meeting does to a direction = 2 - ANNIHILATION marks both ends bounced, and a bounce is resolved through OPP - the opposite exit. Going back the way you came is a REVERSAL: order two, which is a reflection. A reflection has no antisymmetric part, so there is no cross product in it and no perpendicular force
- turns that bring a direction home = 4 - applying G^XOR*2's own turn about an axis, exit 0 goes 0 -> 8 -> 11 -> 3 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 1 - turning exit 0 about [0,0,1] gives 8 and about [1,0,0] gives 1 - different, so the rule sweeps the axis it was given
- \Sigma \hat{d} \otimes \hat{d} = 4.0000 - the exits' second moment on the unit vectors, summed over all 12 of them: 4.000000 against DEG/D = 4.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 6 - 6, against the 4 walked about this axis - DIFFERENT, and both are right. The geometry's CYCLE is the length of its default ring; the order here is the ring about the axis this turn was handed. That a turn's order depends on which axis it is about is a fact about fcc-12 worth having, and it is the reason this quantity is not called CYCLE

**`cubic-6`**

`turning/is-a-turn-a-rotation` - holds. G^XOR*2's alike meeting REVERSES a direction - ANNIHILATION marks it bounced and a bounce resolves through the opposite exit, which is a reflection of order two. A reflection's matrix is symmetric and has no antisymmetric part, so there is no cross product in the dynamics and no perpendicular force: not a small one, none. The lattice DOES carry a ring - a direction comes home after 4 turns about a given axis, and the exits' second moment is 2.0000 = DEG/D, isotropic to 5.6e-16 - which is everything the rotation route would be made of. No rule here consults it. That is why a magnetic force in this implementation needs a direction carried on the ray by hand, which is what G^LABELLED adds and why it exists

- what an alike meeting does to a direction = 2 - ANNIHILATION marks both ends bounced, and a bounce is resolved through OPP - the opposite exit. Going back the way you came is a REVERSAL: order two, which is a reflection. A reflection has no antisymmetric part, so there is no cross product in it and no perpendicular force
- turns that bring a direction home = 4 - applying G^XOR*2's own turn about an axis, exit 0 goes 0 -> 1 -> 5 -> 4 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 1 - turning exit 0 about [0,0,1] gives 1 and about [1,0,0] gives 0 - different, so the rule sweeps the axis it was given
- \Sigma \hat{d} \otimes \hat{d} = 2 - the exits' second moment on the unit vectors, summed over all 6 of them: 2.000000 against DEG/D = 2.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 4 - the same as the order walked here, so the default ring and the ring about this axis are one ring

**`square-4`**

`turning/is-a-turn-a-rotation` - holds. G^XOR*2's alike meeting REVERSES a direction - ANNIHILATION marks it bounced and a bounce resolves through the opposite exit, which is a reflection of order two. A reflection's matrix is symmetric and has no antisymmetric part, so there is no cross product in the dynamics and no perpendicular force: not a small one, none. The lattice DOES carry a ring - a direction comes home after 4 turns about a given axis, and the exits' second moment is 2.0000 = DEG/D, isotropic to 4.4e-16 - which is everything the rotation route would be made of. No rule here consults it. That is why a magnetic force in this implementation needs a direction carried on the ray by hand, which is what G^LABELLED adds and why it exists

- what an alike meeting does to a direction = 2 - ANNIHILATION marks both ends bounced, and a bounce is resolved through OPP - the opposite exit. Going back the way you came is a REVERSAL: order two, which is a reflection. A reflection has no antisymmetric part, so there is no cross product in it and no perpendicular force
- turns that bring a direction home = 4 - applying G^XOR*2's own turn about an axis, exit 0 goes 0 -> 1 -> 3 -> 2 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 0 - both axes give 1, so the rule is not using the axis it was handed and there is no free direction in it for a field to lie along
- \Sigma \hat{d} \otimes \hat{d} = 2 - the exits' second moment on the unit vectors, summed over all 4 of them: 2.000000 against DEG/D = 2.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 4 - the same as the order walked here, so the default ring and the ring about this axis are one ring

### result 2 of 2

**no law follows for SPIN about \hat{b}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- turn order = 0

#### the derivation

There isn't one - the rules could reach no law from what the probes found.

#### what the runs found

`turning/is-a-turn-a-rotation` - does not hold. a turn is a turn in a PLANE and line-2 has 1 dimension, so there is no plane to turn in and nothing here can be a rotation


## G^XOR · relaxing(2/1)

### result 1 of 2

**SPIN = 1/2·\pi**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |

#### the derivation

**SPIN = \frac{2\pi}{turn order}**  
<sub>premise · turning/is-a-turn-a-rotation</sub>  

one turn is one step of a ring of 4, so it is a whole turn over 4 - the phase quantum, and a count of the tiling rather than an angle anybody chose

**turn order = 4**  
<sub>premise · turning/is-a-turn-a-rotation</sub>  

the theory's own turn was applied to a direction until it came home, which took 4 applications: 0 -> 8 -> 11 -> 3 -> 0. That count is the ring's length. It is also the proof that a turn is a ROTATION rather than a reflection, since a reflection has order two - and everything magnetic in this model rests on that distinction

**turn order = 4**  
<sub>derived · a number is an expression</sub>  

turn order is 4, so it can stand in an expression as that

**SPIN = 1/2·\pi**  
<sub>derived · substituting</sub>  

```
SPIN = 2·\frac{\pi}{turn order}
turn order = 4
SPIN = 1/2·\pi
```

turn order is not a primitive of this theory - it is 4, so it stands in for itself here and the result is multiplied out

#### what the runs found

**`fcc-12`**

`turning/is-a-turn-a-rotation` - holds. G^XOR · relaxing(2/1)'s alike meeting REVERSES a direction - ANNIHILATION marks it bounced and a bounce resolves through the opposite exit, which is a reflection of order two. A reflection's matrix is symmetric and has no antisymmetric part, so there is no cross product in the dynamics and no perpendicular force: not a small one, none. The lattice DOES carry a ring - a direction comes home after 4 turns about a given axis, and the exits' second moment is 4.0000 = DEG/D, isotropic to 7.8e-16 - which is everything the rotation route would be made of. No rule here consults it. That is why a magnetic force in this implementation needs a direction carried on the ray by hand, which is what G^LABELLED adds and why it exists

- what an alike meeting does to a direction = 2 - ANNIHILATION marks both ends bounced, and a bounce is resolved through OPP - the opposite exit. Going back the way you came is a REVERSAL: order two, which is a reflection. A reflection has no antisymmetric part, so there is no cross product in it and no perpendicular force
- turns that bring a direction home = 4 - applying G^XOR · relaxing(2/1)'s own turn about an axis, exit 0 goes 0 -> 8 -> 11 -> 3 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 1 - turning exit 0 about [0,0,1] gives 8 and about [1,0,0] gives 1 - different, so the rule sweeps the axis it was given
- \Sigma \hat{d} \otimes \hat{d} = 4.0000 - the exits' second moment on the unit vectors, summed over all 12 of them: 4.000000 against DEG/D = 4.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 6 - 6, against the 4 walked about this axis - DIFFERENT, and both are right. The geometry's CYCLE is the length of its default ring; the order here is the ring about the axis this turn was handed. That a turn's order depends on which axis it is about is a fact about fcc-12 worth having, and it is the reason this quantity is not called CYCLE

**`cubic-6`**

`turning/is-a-turn-a-rotation` - holds. G^XOR · relaxing(2/1)'s alike meeting REVERSES a direction - ANNIHILATION marks it bounced and a bounce resolves through the opposite exit, which is a reflection of order two. A reflection's matrix is symmetric and has no antisymmetric part, so there is no cross product in the dynamics and no perpendicular force: not a small one, none. The lattice DOES carry a ring - a direction comes home after 4 turns about a given axis, and the exits' second moment is 2.0000 = DEG/D, isotropic to 5.6e-16 - which is everything the rotation route would be made of. No rule here consults it. That is why a magnetic force in this implementation needs a direction carried on the ray by hand, which is what G^LABELLED adds and why it exists

- what an alike meeting does to a direction = 2 - ANNIHILATION marks both ends bounced, and a bounce is resolved through OPP - the opposite exit. Going back the way you came is a REVERSAL: order two, which is a reflection. A reflection has no antisymmetric part, so there is no cross product in it and no perpendicular force
- turns that bring a direction home = 4 - applying G^XOR · relaxing(2/1)'s own turn about an axis, exit 0 goes 0 -> 1 -> 5 -> 4 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 1 - turning exit 0 about [0,0,1] gives 1 and about [1,0,0] gives 0 - different, so the rule sweeps the axis it was given
- \Sigma \hat{d} \otimes \hat{d} = 2 - the exits' second moment on the unit vectors, summed over all 6 of them: 2.000000 against DEG/D = 2.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 4 - the same as the order walked here, so the default ring and the ring about this axis are one ring

**`square-4`**

`turning/is-a-turn-a-rotation` - holds. G^XOR · relaxing(2/1)'s alike meeting REVERSES a direction - ANNIHILATION marks it bounced and a bounce resolves through the opposite exit, which is a reflection of order two. A reflection's matrix is symmetric and has no antisymmetric part, so there is no cross product in the dynamics and no perpendicular force: not a small one, none. The lattice DOES carry a ring - a direction comes home after 4 turns about a given axis, and the exits' second moment is 2.0000 = DEG/D, isotropic to 4.4e-16 - which is everything the rotation route would be made of. No rule here consults it. That is why a magnetic force in this implementation needs a direction carried on the ray by hand, which is what G^LABELLED adds and why it exists

- what an alike meeting does to a direction = 2 - ANNIHILATION marks both ends bounced, and a bounce is resolved through OPP - the opposite exit. Going back the way you came is a REVERSAL: order two, which is a reflection. A reflection has no antisymmetric part, so there is no cross product in it and no perpendicular force
- turns that bring a direction home = 4 - applying G^XOR · relaxing(2/1)'s own turn about an axis, exit 0 goes 0 -> 1 -> 3 -> 2 and back to 0 after 4 of them. An order of two would be a reflection; 4 is a rotation
- does the turn use the axis it is handed = 0 - both axes give 1, so the rule is not using the axis it was handed and there is no free direction in it for a field to lie along
- \Sigma \hat{d} \otimes \hat{d} = 2 - the exits' second moment on the unit vectors, summed over all 4 of them: 2.000000 against DEG/D = 2.000000, and the whole tensor is isotropic. Read off the exit vectors, with nothing running
- the geometry's own CYCLE, for comparison = 4 - the same as the order walked here, so the default ring and the ring about this axis are one ring

### result 2 of 2

**no law follows for SPIN about \hat{b}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- turn order = 0

#### the derivation

There isn't one - the rules could reach no law from what the probes found.

#### what the runs found

`turning/is-a-turn-a-rotation` - does not hold. a turn is a turn in a PLANE and line-2 has 1 dimension, so there is no plane to turn in and nothing here can be a rotation

