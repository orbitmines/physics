# space.recession

> the vacuum makes space wherever it is idle. What does that do to two things sitting some distance apart?

Proved under `G`, `G^XOR`, `G^XOR*2`. Generated by `implementations/.ts/src/theorems` - nothing here was written by hand: every premise is a count or an exact invariance taken from the theory itself, and the conclusion is what the inference rules made of them. [Open the page](index.html) to switch theory and lattice.

| theory | answer |
|---|---|
| `G` | recession ∝ separation |
| `G^XOR` | recession ∝ separation |
| `G^XOR*2` | recession ∝ separation |

## G

### the result

**recession ∝ separation**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**recession ∝ separation**  
<sub>premise · expanding/why-far-things-recede-faster</sub>  

space is made by idle points splitting, and the only points that can add to the distance between two markers are the ones BETWEEN them - each contributing at most one step when it splits. So how fast they come apart is how many such points there are, times whatever rate each splits at; and the count is proportional to the separation. The rate per point is the vacuum's own business and is not claimed here - what is claimed is that it is the SAME rate everywhere, so the recession goes as the separation with one constant for the whole medium. That is Hubble's law, and it is a count of what lies between rather than a statement about the universe

#### what the runs found

**`fcc-12`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on fcc-12 go 1, 2, 3, 4, 5 at separations 2, 3, 4, 5, 6 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6 on fcc-12: 1, 2, 3, 4, 5. Differences 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked

**`cubic-6`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on cubic-6 go 1, 2, 3, 4, 5 at separations 2, 3, 4, 5, 6 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6 on cubic-6: 1, 2, 3, 4, 5. Differences 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked

**`square-4`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on square-4 go 1, 2, 3, 4, 5, 6, 7 at separations 2, 3, 4, 5, 6, 7, 8 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6, 7, 8 on square-4: 1, 2, 3, 4, 5, 6, 7. Differences 1, 1, 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked

**`line-2`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on line-2 go 1, 2, 3, 4, 5, 6, 7 at separations 2, 3, 4, 5, 6, 7, 8 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6, 7, 8 on line-2: 1, 2, 3, 4, 5, 6, 7. Differences 1, 1, 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked

## G^XOR

### the result

**recession ∝ separation**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**recession ∝ separation**  
<sub>premise · expanding/why-far-things-recede-faster</sub>  

space is made by idle points splitting, and the only points that can add to the distance between two markers are the ones BETWEEN them - each contributing at most one step when it splits. So how fast they come apart is how many such points there are, times whatever rate each splits at; and the count is proportional to the separation. The rate per point is the vacuum's own business and is not claimed here - what is claimed is that it is the SAME rate everywhere, so the recession goes as the separation with one constant for the whole medium. That is Hubble's law, and it is a count of what lies between rather than a statement about the universe

#### what the runs found

**`fcc-12`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on fcc-12 go 1, 2, 3, 4, 5 at separations 2, 3, 4, 5, 6 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6 on fcc-12: 1, 2, 3, 4, 5. Differences 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked

**`cubic-6`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on cubic-6 go 1, 2, 3, 4, 5 at separations 2, 3, 4, 5, 6 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6 on cubic-6: 1, 2, 3, 4, 5. Differences 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked

**`square-4`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on square-4 go 1, 2, 3, 4, 5, 6, 7 at separations 2, 3, 4, 5, 6, 7, 8 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6, 7, 8 on square-4: 1, 2, 3, 4, 5, 6, 7. Differences 1, 1, 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked

**`line-2`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on line-2 go 1, 2, 3, 4, 5, 6, 7 at separations 2, 3, 4, 5, 6, 7, 8 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6, 7, 8 on line-2: 1, 2, 3, 4, 5, 6, 7. Differences 1, 1, 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked

## G^XOR*2

### the result

**recession ∝ separation**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**recession ∝ separation**  
<sub>premise · expanding/why-far-things-recede-faster</sub>  

space is made by idle points splitting, and the only points that can add to the distance between two markers are the ones BETWEEN them - each contributing at most one step when it splits. So how fast they come apart is how many such points there are, times whatever rate each splits at; and the count is proportional to the separation. The rate per point is the vacuum's own business and is not claimed here - what is claimed is that it is the SAME rate everywhere, so the recession goes as the separation with one constant for the whole medium. That is Hubble's law, and it is a count of what lies between rather than a statement about the universe

#### what the runs found

**`fcc-12`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on fcc-12 go 1, 2, 3, 4, 5 at separations 2, 3, 4, 5, 6 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6 on fcc-12: 1, 2, 3, 4, 5. Differences 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked

**`cubic-6`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on cubic-6 go 1, 2, 3, 4, 5 at separations 2, 3, 4, 5, 6 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6 on cubic-6: 1, 2, 3, 4, 5. Differences 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked

**`square-4`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on square-4 go 1, 2, 3, 4, 5, 6, 7 at separations 2, 3, 4, 5, 6, 7, 8 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6, 7, 8 on square-4: 1, 2, 3, 4, 5, 6, 7. Differences 1, 1, 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked

**`line-2`**

`expanding/why-far-things-recede-faster` - holds. the points between two markers on line-2 go 1, 2, 3, 4, 5, 6, 7 at separations 2, 3, 4, 5, 6, 7, 8 - proportional, exactly. Each of them can add a step when it splits and nothing else can, so two markers come apart at a rate proportional to how far apart they already are

- idle points between = 1 - points lying between two markers at separations 2, 3, 4, 5, 6, 7, 8 on line-2: 1, 2, 3, 4, 5, 6, 7. Differences 1, 1, 1, 1, 1, 1, which are constant at 1, so the count is linear in the separation. Walked over the lattice's own positions, with nothing ticked
