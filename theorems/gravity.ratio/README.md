# gravity.ratio

> mass goes as one over size, so a mass ratio is a size ratio. What are the two sizes a bound pair has here, and what does the ratio between them come to?

Proved under `G`, `G^XOR`, `G^XOR*2`, `G^XOR^c`, `G^XOR+XOR`, `G^XOR^o`, `G^XOR · relaxing(2/1)`. Generated by `implementations/.ts/src/theorems` - nothing here was written by hand: every premise is a count or an exact invariance taken from the theory itself, and the conclusion is what the inference rules made of them. [Open the page](index.html) to switch theory and lattice.

| theory | answer |
|---|---|
| `G` | \frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE} |
| `G^XOR` | \frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE} |
| `G^XOR*2` | \frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE} |
| `G^XOR^c` | \frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE} |
| `G^XOR+XOR` | \frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE} |
| `G^XOR^o` | \frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE} |
| `G^XOR · relaxing(2/1)` | \frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE} |

## G

### the result

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- \lambda > 0

#### the derivation

**\frac{m_p}{m_e} = \frac{r_e}{r_p} = \frac{2\pi·\lambda}{CYCLE}**  
<sub>definition · gravity.ratio</sub>  

AND A MASS RATIO IS A SIZE RATIO INVERTED, which is `gravity.atom`'s m ∝ 1/r and nothing more. Both masses are the same kind of thing - an amount of turning - so the constant between mass and size is the same for both and cancels. What is left is 2πλ/CYCLE, and every symbol in it is a count this folder already derives

**r_p = \frac{CYCLE}{2\pi}**  
<sub>definition · gravity.ratio</sub>  

THE TIGHTEST THING THAT CAN CLOSE is a lap, and a lap is CYCLE ring steps - `G^XOR^o`'s `laps`, and `lattice.turn` establishes that CYCLE does not grow with the dimension. A closed curve whose circumference is CYCLE cells has radius CYCLE/(2π), and nothing smaller closes because there is no shorter way round the ring. The 2π is not fitted; it is what a circumference is

**\frac{m_p}{m_e} ∝ \frac{r_e}{CYCLE}**  
<sub>derived · expansion</sub>  

r_p is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**r_e = \lambda**  
<sub>definition · gravity.ratio</sub>  

AND THE CLOUD REACHES AS FAR AS ITS CARRIERS DO. `gravity.atom` establishes that every shell of it weighs the same, so its mass is set by HOW MANY SHELLS there are and diverges without a cut - and what cuts it is the carrier being eaten, which `reach.range` derives from the vacuum's occupancy rather than assuming

**r_e ∝ \lambda**  
<sub>derived · which is also a scaling</sub>  

r_e is \lambda, which is a single product of powers - so it scales as that, and whatever was written in terms of r_e can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{\lambda}{CYCLE}**  
<sub>derived · expansion</sub>  

r_e is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**death per step = partner · fatal fraction**  
<sub>premise · survival/what-kills-a-carrier</sub>  

ANNIHILATION is quantified over a facing pair and gated on "active", read off G itself rather than transcribed. So a carrier is destroyed on a step exactly when there is something facing it carrying something - which is how often partner - AND that meeting is one of the ones that leaves nothing - which is fatal fraction. Neither alone kills anything. ASSUMED HERE, and nowhere else: that whether a partner is present is not correlated with whether the carrier is, which is the standard kinetic assumption and is what lets the two be multiplied

**\lambda = -1/ln(1 - death per step)**  
<sub>derived · a chance per step compounds</sub>  

and the length it dies over is that chance's own logarithm. This is a transcendental in a folder of counts, and deliberately so: death per step is a ratio of counts and its logarithm is not, but it is a CLOSED FORM which names exactly which counts it came from - which is what distinguishes it from a fitted parameter. A theory that destroys nothing has death per step = 0 and a range that is infinite, so its forces are not screened at all

**\lambda ∝ -1/ln(1 - death per step)**  
<sub>derived · which is also a scaling</sub>  

\lambda is -1/ln(1 - death per step), which is a single product of powers - so it scales as that, and whatever was written in terms of \lambda can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**  
<sub>derived · expansion</sub>  

\lambda is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

## G^XOR

### the result

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- \lambda > 0

#### the derivation

**\frac{m_p}{m_e} = \frac{r_e}{r_p} = \frac{2\pi·\lambda}{CYCLE}**  
<sub>definition · gravity.ratio</sub>  

AND A MASS RATIO IS A SIZE RATIO INVERTED, which is `gravity.atom`'s m ∝ 1/r and nothing more. Both masses are the same kind of thing - an amount of turning - so the constant between mass and size is the same for both and cancels. What is left is 2πλ/CYCLE, and every symbol in it is a count this folder already derives

**r_p = \frac{CYCLE}{2\pi}**  
<sub>definition · gravity.ratio</sub>  

THE TIGHTEST THING THAT CAN CLOSE is a lap, and a lap is CYCLE ring steps - `G^XOR^o`'s `laps`, and `lattice.turn` establishes that CYCLE does not grow with the dimension. A closed curve whose circumference is CYCLE cells has radius CYCLE/(2π), and nothing smaller closes because there is no shorter way round the ring. The 2π is not fitted; it is what a circumference is

**\frac{m_p}{m_e} ∝ \frac{r_e}{CYCLE}**  
<sub>derived · expansion</sub>  

r_p is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**r_e = \lambda**  
<sub>definition · gravity.ratio</sub>  

AND THE CLOUD REACHES AS FAR AS ITS CARRIERS DO. `gravity.atom` establishes that every shell of it weighs the same, so its mass is set by HOW MANY SHELLS there are and diverges without a cut - and what cuts it is the carrier being eaten, which `reach.range` derives from the vacuum's occupancy rather than assuming

**r_e ∝ \lambda**  
<sub>derived · which is also a scaling</sub>  

r_e is \lambda, which is a single product of powers - so it scales as that, and whatever was written in terms of r_e can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{\lambda}{CYCLE}**  
<sub>derived · expansion</sub>  

r_e is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**death per step = partner · fatal fraction**  
<sub>premise · survival/what-kills-a-carrier</sub>  

ANNIHILATION is quantified over a facing pair and gated on "active", read off G^XOR itself rather than transcribed. So a carrier is destroyed on a step exactly when there is something facing it carrying something - which is how often partner - AND that meeting is one of the ones that leaves nothing - which is fatal fraction. Neither alone kills anything. ASSUMED HERE, and nowhere else: that whether a partner is present is not correlated with whether the carrier is, which is the standard kinetic assumption and is what lets the two be multiplied

**\lambda = -1/ln(1 - death per step)**  
<sub>derived · a chance per step compounds</sub>  

and the length it dies over is that chance's own logarithm. This is a transcendental in a folder of counts, and deliberately so: death per step is a ratio of counts and its logarithm is not, but it is a CLOSED FORM which names exactly which counts it came from - which is what distinguishes it from a fitted parameter. A theory that destroys nothing has death per step = 0 and a range that is infinite, so its forces are not screened at all

**\lambda ∝ -1/ln(1 - death per step)**  
<sub>derived · which is also a scaling</sub>  

\lambda is -1/ln(1 - death per step), which is a single product of powers - so it scales as that, and whatever was written in terms of \lambda can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**  
<sub>derived · expansion</sub>  

\lambda is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

## G^XOR*2

### the result

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- \lambda > 0

#### the derivation

**\frac{m_p}{m_e} = \frac{r_e}{r_p} = \frac{2\pi·\lambda}{CYCLE}**  
<sub>definition · gravity.ratio</sub>  

AND A MASS RATIO IS A SIZE RATIO INVERTED, which is `gravity.atom`'s m ∝ 1/r and nothing more. Both masses are the same kind of thing - an amount of turning - so the constant between mass and size is the same for both and cancels. What is left is 2πλ/CYCLE, and every symbol in it is a count this folder already derives

**r_p = \frac{CYCLE}{2\pi}**  
<sub>definition · gravity.ratio</sub>  

THE TIGHTEST THING THAT CAN CLOSE is a lap, and a lap is CYCLE ring steps - `G^XOR^o`'s `laps`, and `lattice.turn` establishes that CYCLE does not grow with the dimension. A closed curve whose circumference is CYCLE cells has radius CYCLE/(2π), and nothing smaller closes because there is no shorter way round the ring. The 2π is not fitted; it is what a circumference is

**\frac{m_p}{m_e} ∝ \frac{r_e}{CYCLE}**  
<sub>derived · expansion</sub>  

r_p is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**r_e = \lambda**  
<sub>definition · gravity.ratio</sub>  

AND THE CLOUD REACHES AS FAR AS ITS CARRIERS DO. `gravity.atom` establishes that every shell of it weighs the same, so its mass is set by HOW MANY SHELLS there are and diverges without a cut - and what cuts it is the carrier being eaten, which `reach.range` derives from the vacuum's occupancy rather than assuming

**r_e ∝ \lambda**  
<sub>derived · which is also a scaling</sub>  

r_e is \lambda, which is a single product of powers - so it scales as that, and whatever was written in terms of r_e can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{\lambda}{CYCLE}**  
<sub>derived · expansion</sub>  

r_e is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**death per step = partner · fatal fraction**  
<sub>premise · survival/what-kills-a-carrier</sub>  

ANNIHILATION is quantified over a facing pair and gated on "active", read off G^XOR*2 itself rather than transcribed. So a carrier is destroyed on a step exactly when there is something facing it carrying something - which is how often partner - AND that meeting is one of the ones that leaves nothing - which is fatal fraction. Neither alone kills anything. ASSUMED HERE, and nowhere else: that whether a partner is present is not correlated with whether the carrier is, which is the standard kinetic assumption and is what lets the two be multiplied

**\lambda = -1/ln(1 - death per step)**  
<sub>derived · a chance per step compounds</sub>  

and the length it dies over is that chance's own logarithm. This is a transcendental in a folder of counts, and deliberately so: death per step is a ratio of counts and its logarithm is not, but it is a CLOSED FORM which names exactly which counts it came from - which is what distinguishes it from a fitted parameter. A theory that destroys nothing has death per step = 0 and a range that is infinite, so its forces are not screened at all

**\lambda ∝ -1/ln(1 - death per step)**  
<sub>derived · which is also a scaling</sub>  

\lambda is -1/ln(1 - death per step), which is a single product of powers - so it scales as that, and whatever was written in terms of \lambda can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**  
<sub>derived · expansion</sub>  

\lambda is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR*2 is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR*2 is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR*2 is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR*2 is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

## G^XOR^c

### the result

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- \lambda > 0

#### the derivation

**\frac{m_p}{m_e} = \frac{r_e}{r_p} = \frac{2\pi·\lambda}{CYCLE}**  
<sub>definition · gravity.ratio</sub>  

AND A MASS RATIO IS A SIZE RATIO INVERTED, which is `gravity.atom`'s m ∝ 1/r and nothing more. Both masses are the same kind of thing - an amount of turning - so the constant between mass and size is the same for both and cancels. What is left is 2πλ/CYCLE, and every symbol in it is a count this folder already derives

**r_p = \frac{CYCLE}{2\pi}**  
<sub>definition · gravity.ratio</sub>  

THE TIGHTEST THING THAT CAN CLOSE is a lap, and a lap is CYCLE ring steps - `G^XOR^o`'s `laps`, and `lattice.turn` establishes that CYCLE does not grow with the dimension. A closed curve whose circumference is CYCLE cells has radius CYCLE/(2π), and nothing smaller closes because there is no shorter way round the ring. The 2π is not fitted; it is what a circumference is

**\frac{m_p}{m_e} ∝ \frac{r_e}{CYCLE}**  
<sub>derived · expansion</sub>  

r_p is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**r_e = \lambda**  
<sub>definition · gravity.ratio</sub>  

AND THE CLOUD REACHES AS FAR AS ITS CARRIERS DO. `gravity.atom` establishes that every shell of it weighs the same, so its mass is set by HOW MANY SHELLS there are and diverges without a cut - and what cuts it is the carrier being eaten, which `reach.range` derives from the vacuum's occupancy rather than assuming

**r_e ∝ \lambda**  
<sub>derived · which is also a scaling</sub>  

r_e is \lambda, which is a single product of powers - so it scales as that, and whatever was written in terms of r_e can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{\lambda}{CYCLE}**  
<sub>derived · expansion</sub>  

r_e is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**death per step = partner · fatal fraction**  
<sub>premise · survival/what-kills-a-carrier</sub>  

ANNIHILATION is quantified over a facing pair and gated on "active", read off G^XOR^c itself rather than transcribed. So a carrier is destroyed on a step exactly when there is something facing it carrying something - which is how often partner - AND that meeting is one of the ones that leaves nothing - which is fatal fraction. Neither alone kills anything. ASSUMED HERE, and nowhere else: that whether a partner is present is not correlated with whether the carrier is, which is the standard kinetic assumption and is what lets the two be multiplied

**\lambda = -1/ln(1 - death per step)**  
<sub>derived · a chance per step compounds</sub>  

and the length it dies over is that chance's own logarithm. This is a transcendental in a folder of counts, and deliberately so: death per step is a ratio of counts and its logarithm is not, but it is a CLOSED FORM which names exactly which counts it came from - which is what distinguishes it from a fitted parameter. A theory that destroys nothing has death per step = 0 and a range that is infinite, so its forces are not screened at all

**\lambda ∝ -1/ln(1 - death per step)**  
<sub>derived · which is also a scaling</sub>  

\lambda is -1/ln(1 - death per step), which is a single product of powers - so it scales as that, and whatever was written in terms of \lambda can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**  
<sub>derived · expansion</sub>  

\lambda is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR^c is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR^c is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR^c is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR^c is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

## G^XOR+XOR

### the result

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- \lambda > 0

#### the derivation

**\frac{m_p}{m_e} = \frac{r_e}{r_p} = \frac{2\pi·\lambda}{CYCLE}**  
<sub>definition · gravity.ratio</sub>  

AND A MASS RATIO IS A SIZE RATIO INVERTED, which is `gravity.atom`'s m ∝ 1/r and nothing more. Both masses are the same kind of thing - an amount of turning - so the constant between mass and size is the same for both and cancels. What is left is 2πλ/CYCLE, and every symbol in it is a count this folder already derives

**r_p = \frac{CYCLE}{2\pi}**  
<sub>definition · gravity.ratio</sub>  

THE TIGHTEST THING THAT CAN CLOSE is a lap, and a lap is CYCLE ring steps - `G^XOR^o`'s `laps`, and `lattice.turn` establishes that CYCLE does not grow with the dimension. A closed curve whose circumference is CYCLE cells has radius CYCLE/(2π), and nothing smaller closes because there is no shorter way round the ring. The 2π is not fitted; it is what a circumference is

**\frac{m_p}{m_e} ∝ \frac{r_e}{CYCLE}**  
<sub>derived · expansion</sub>  

r_p is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**r_e = \lambda**  
<sub>definition · gravity.ratio</sub>  

AND THE CLOUD REACHES AS FAR AS ITS CARRIERS DO. `gravity.atom` establishes that every shell of it weighs the same, so its mass is set by HOW MANY SHELLS there are and diverges without a cut - and what cuts it is the carrier being eaten, which `reach.range` derives from the vacuum's occupancy rather than assuming

**r_e ∝ \lambda**  
<sub>derived · which is also a scaling</sub>  

r_e is \lambda, which is a single product of powers - so it scales as that, and whatever was written in terms of r_e can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{\lambda}{CYCLE}**  
<sub>derived · expansion</sub>  

r_e is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**death per step = partner · fatal fraction**  
<sub>premise · survival/what-kills-a-carrier</sub>  

ANNIHILATION is quantified over a facing pair and gated on "active", read off G^XOR+XOR itself rather than transcribed. So a carrier is destroyed on a step exactly when there is something facing it carrying something - which is how often partner - AND that meeting is one of the ones that leaves nothing - which is fatal fraction. Neither alone kills anything. ASSUMED HERE, and nowhere else: that whether a partner is present is not correlated with whether the carrier is, which is the standard kinetic assumption and is what lets the two be multiplied

**\lambda = -1/ln(1 - death per step)**  
<sub>derived · a chance per step compounds</sub>  

and the length it dies over is that chance's own logarithm. This is a transcendental in a folder of counts, and deliberately so: death per step is a ratio of counts and its logarithm is not, but it is a CLOSED FORM which names exactly which counts it came from - which is what distinguishes it from a fitted parameter. A theory that destroys nothing has death per step = 0 and a range that is infinite, so its forces are not screened at all

**\lambda ∝ -1/ln(1 - death per step)**  
<sub>derived · which is also a scaling</sub>  

\lambda is -1/ln(1 - death per step), which is a single product of powers - so it scales as that, and whatever was written in terms of \lambda can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**  
<sub>derived · expansion</sub>  

\lambda is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR+XOR is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR+XOR is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR+XOR is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR+XOR is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

## G^XOR^o

### the result

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- \lambda > 0

#### the derivation

**\frac{m_p}{m_e} = \frac{r_e}{r_p} = \frac{2\pi·\lambda}{CYCLE}**  
<sub>definition · gravity.ratio</sub>  

AND A MASS RATIO IS A SIZE RATIO INVERTED, which is `gravity.atom`'s m ∝ 1/r and nothing more. Both masses are the same kind of thing - an amount of turning - so the constant between mass and size is the same for both and cancels. What is left is 2πλ/CYCLE, and every symbol in it is a count this folder already derives

**r_p = \frac{CYCLE}{2\pi}**  
<sub>definition · gravity.ratio</sub>  

THE TIGHTEST THING THAT CAN CLOSE is a lap, and a lap is CYCLE ring steps - `G^XOR^o`'s `laps`, and `lattice.turn` establishes that CYCLE does not grow with the dimension. A closed curve whose circumference is CYCLE cells has radius CYCLE/(2π), and nothing smaller closes because there is no shorter way round the ring. The 2π is not fitted; it is what a circumference is

**\frac{m_p}{m_e} ∝ \frac{r_e}{CYCLE}**  
<sub>derived · expansion</sub>  

r_p is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**r_e = \lambda**  
<sub>definition · gravity.ratio</sub>  

AND THE CLOUD REACHES AS FAR AS ITS CARRIERS DO. `gravity.atom` establishes that every shell of it weighs the same, so its mass is set by HOW MANY SHELLS there are and diverges without a cut - and what cuts it is the carrier being eaten, which `reach.range` derives from the vacuum's occupancy rather than assuming

**r_e ∝ \lambda**  
<sub>derived · which is also a scaling</sub>  

r_e is \lambda, which is a single product of powers - so it scales as that, and whatever was written in terms of r_e can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{\lambda}{CYCLE}**  
<sub>derived · expansion</sub>  

r_e is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**death per step = partner · fatal fraction**  
<sub>premise · survival/what-kills-a-carrier</sub>  

ANNIHILATION is quantified over a facing pair and gated on "active", read off G^XOR^o itself rather than transcribed. So a carrier is destroyed on a step exactly when there is something facing it carrying something - which is how often partner - AND that meeting is one of the ones that leaves nothing - which is fatal fraction. Neither alone kills anything. ASSUMED HERE, and nowhere else: that whether a partner is present is not correlated with whether the carrier is, which is the standard kinetic assumption and is what lets the two be multiplied

**\lambda = -1/ln(1 - death per step)**  
<sub>derived · a chance per step compounds</sub>  

and the length it dies over is that chance's own logarithm. This is a transcendental in a folder of counts, and deliberately so: death per step is a ratio of counts and its logarithm is not, but it is a CLOSED FORM which names exactly which counts it came from - which is what distinguishes it from a fitted parameter. A theory that destroys nothing has death per step = 0 and a range that is infinite, so its forces are not screened at all

**\lambda ∝ -1/ln(1 - death per step)**  
<sub>derived · which is also a scaling</sub>  

\lambda is -1/ln(1 - death per step), which is a single product of powers - so it scales as that, and whatever was written in terms of \lambda can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**  
<sub>derived · expansion</sub>  

\lambda is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR^o is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR^o is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR^o is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR^o is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

## G^XOR · relaxing(2/1)

### the result

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- \lambda > 0

#### the derivation

**\frac{m_p}{m_e} = \frac{r_e}{r_p} = \frac{2\pi·\lambda}{CYCLE}**  
<sub>definition · gravity.ratio</sub>  

AND A MASS RATIO IS A SIZE RATIO INVERTED, which is `gravity.atom`'s m ∝ 1/r and nothing more. Both masses are the same kind of thing - an amount of turning - so the constant between mass and size is the same for both and cancels. What is left is 2πλ/CYCLE, and every symbol in it is a count this folder already derives

**r_p = \frac{CYCLE}{2\pi}**  
<sub>definition · gravity.ratio</sub>  

THE TIGHTEST THING THAT CAN CLOSE is a lap, and a lap is CYCLE ring steps - `G^XOR^o`'s `laps`, and `lattice.turn` establishes that CYCLE does not grow with the dimension. A closed curve whose circumference is CYCLE cells has radius CYCLE/(2π), and nothing smaller closes because there is no shorter way round the ring. The 2π is not fitted; it is what a circumference is

**\frac{m_p}{m_e} ∝ \frac{r_e}{CYCLE}**  
<sub>derived · expansion</sub>  

r_p is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**r_e = \lambda**  
<sub>definition · gravity.ratio</sub>  

AND THE CLOUD REACHES AS FAR AS ITS CARRIERS DO. `gravity.atom` establishes that every shell of it weighs the same, so its mass is set by HOW MANY SHELLS there are and diverges without a cut - and what cuts it is the carrier being eaten, which `reach.range` derives from the vacuum's occupancy rather than assuming

**r_e ∝ \lambda**  
<sub>derived · which is also a scaling</sub>  

r_e is \lambda, which is a single product of powers - so it scales as that, and whatever was written in terms of r_e can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{\lambda}{CYCLE}**  
<sub>derived · expansion</sub>  

r_e is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

**death per step = partner · fatal fraction**  
<sub>premise · survival/what-kills-a-carrier</sub>  

ANNIHILATION is quantified over a facing pair and gated on "active", read off G^XOR · relaxing(2/1) itself rather than transcribed. So a carrier is destroyed on a step exactly when there is something facing it carrying something - which is how often partner - AND that meeting is one of the ones that leaves nothing - which is fatal fraction. Neither alone kills anything. ASSUMED HERE, and nowhere else: that whether a partner is present is not correlated with whether the carrier is, which is the standard kinetic assumption and is what lets the two be multiplied

**\lambda = -1/ln(1 - death per step)**  
<sub>derived · a chance per step compounds</sub>  

and the length it dies over is that chance's own logarithm. This is a transcendental in a folder of counts, and deliberately so: death per step is a ratio of counts and its logarithm is not, but it is a CLOSED FORM which names exactly which counts it came from - which is what distinguishes it from a fitted parameter. A theory that destroys nothing has death per step = 0 and a range that is infinite, so its forces are not screened at all

**\lambda ∝ -1/ln(1 - death per step)**  
<sub>derived · which is also a scaling</sub>  

\lambda is -1/ln(1 - death per step), which is a single product of powers - so it scales as that, and whatever was written in terms of \lambda can be written in terms of what it is made of

**\frac{m_p}{m_e} ∝ \frac{-1/ln(1 - death per step)}{CYCLE}**  
<sub>derived · expansion</sub>  

\lambda is not a primitive of this theory - it is what the line above shows it to be, so it stands in for itself here

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR · relaxing(2/1) is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR · relaxing(2/1) is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR · relaxing(2/1) is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`survival/what-kills-a-carrier` - holds. ANNIHILATION destroys a carrier only when something facing it is carrying something, so the chance of dying on a step is how often there is a partner times how often such a meeting is fatal - both of them counts this folder already has, so the range of a force in G^XOR · relaxing(2/1) is derived rather than calibrated

- rules that can destroy a carrier in flight = 1 - ANNIHILATION over [Boundary, Boundary] gated on "active"
