# vacuum.equation

> every rule of the theory is a term, and every rule touches two things - the population and the space. What ARE the continuous equations, counted off the rules?

Proved under `G`. Generated by `npx ray gen` from `implementation/physics.ray` - nothing here was written by hand: every premise is read off the rules themselves, and the conclusion is what the inference rules of `Prove.ray` make of them.

| theory | answer |
|---|---|
| `G` | \partial_{t} n + \hat{d} \cdot \nabla_{x} n + \paren{\nabla n_{f}} \cdot \nabla_{\hat{d}} n = - 2 \sigma F n^{2} + \bar{DEG} \nu \paren{1 - \rho^{\bar{DEG}}} - \paren{\Sigma \paren{1 - \beta}} n + \Sigma \paren{\omega \paren{1 - \beta}} |

## G

### the result

**\partial_{t} n + \hat{d} \cdot \nabla_{x} n + \paren{\nabla n_{f}} \cdot \nabla_{\hat{d}} n = - 2 \sigma F n^{2} + \bar{DEG} \nu \paren{1 - \rho^{\bar{DEG}}} - \paren{\Sigma \paren{1 - \beta}} n + \Sigma \paren{\omega \paren{1 - \beta}}**

#### the derivation

**\text{what is made} = \nu \paren{1 - \rho^{\bar{DEG}}}**  
<sub>rule · CREATION</sub>  

```
the rate is ν
its gates let 1 - \rho^{\bar{DEG}} through
its quantifier walks the density to the power 0
```

CREATION makes at \nu \paren{1 - \rho^{\bar{DEG}}} - its rate, times what its gates let through, times the density to the power its quantifier gives

**\text{the rays count of what is made} = \bar{DEG}**  
<sub>rule · CREATION</sub>  

```
rays a firing: \bar{DEG}
```

one firing of CREATION lights \bar{DEG} rays, which is what its body does to the ray ledger

**\text{the ways out of a point} = \bar{DEG}**  
<sub>rule · CREATION</sub>  

```
the body lit DEG exits
so a point has DEG ways out
```

CREATION lights every exit a point has, so the count its body ran over is how many ways out there are - DEG. A shortfall is ways out that are missing, so that count is also its ceiling

**\text{what is taken} = \sigma F \rho^{2}**  
<sub>rule · ANNIHILATION</sub>  

```
the rate is σ
its gates let F through
its quantifier walks the density to the power 2
```

ANNIHILATION takes at \sigma F \rho^{2} - its rate, times what its gates let through, times the density to the power its quantifier gives

**\text{the rays count of what is taken} = -2**  
<sub>rule · ANNIHILATION</sub>  

```
rays a meeting: -2
```

one meeting under ANNIHILATION takes 2 rays, one from each side, which is what its body does to the ray ledger

**\sigma = 1**  
<sub>rule · the rewrite</sub>  

```
σ = 1 per match per tick
```

a rewrite fires on every match it has, once a tick - that is what a rule of this model is. So its rate is ONE per match per tick, and the name on it says which rewrite rather than how often: there is nothing here to choose or to fit

**\nu = 1**  
<sub>rule · the rewrite</sub>  

```
ν = 1 per match per tick
```

a rewrite fires on every match it has, once a tick - that is what a rule of this model is. So its rate is ONE per match per tick, and the name on it says which rewrite rather than how often: there is nothing here to choose or to fit

**\nabla^{2} \rho = 2 \sigma F \rho + -\paren{\nu \paren{0 - \bar{DEG} \paren{\rho^{\paren{\bar{DEG} - 1}}}}} \, \sigma_{tr} \, \rho**  
<sub>theorem · and so the rate</sub>  

```
how annihilation answers a change in the density: 2 \sigma F \rho
how creation answers a change in the density: -\paren{\nu \paren{0 - \bar{DEG} \paren{\rho^{\paren{\bar{DEG} - 1}}}}}
a = 2 \sigma F \rho + -\paren{\nu \paren{0 - \bar{DEG} \paren{\rho^{\paren{\bar{DEG} - 1}}}}}
```

every term that depends on the density answers a change in it, and they do not consult one another - so what the line does back to A CHANGE IN THE DENSITY is their sum

**\delta \text{ is conserved on its way out}**  
<sub>theorem · every rule is a function of its own match</sub>  

```
every one of the 7 terms the line carries is a rule acting on the match it was handed
none of them consults anything outside it, so agreement is carried and disagreement is only moved
the DENSITY is pushed back at a = 2 \sigma F \rho + -\paren{\nu \paren{0 - \bar{DEG} \paren{\rho^{\paren{\bar{DEG} - 1}}}}}, which is a different quantity
```

a rule reads the refs its match handed over and nothing else, so two worlds that agree about a match agree after it - a difference between them can be moved and cannot be made or unmade. What a meeting takes from the disturbed world it leaves as a hole in the undisturbed one, and the difference is the same size. So as much of the shortfall crosses a far shell as a near one, which is what the dilution argument wants

**\delta \text{ goes every way alike}**  
<sub>theorem · the lattice</sub>  

```
exits(p): DEG ways out, none of them special
```

a point's exits are alike and a rule reads none of them by name, so what leaves a place goes every way alike

**\Sigma \text{ goes every way alike}**  
<sub>theorem · put in from outside</sub>  

a body lights its exits alike, so what leaves it goes every way alike

**\bar{c} = 1**  
<sub>theorem · MOVEMENT</sub>  

```
\bar{c} = 1 cell per tick
```

a ray goes one cell a tick - that is what the transport rule does, and it is the only speed there is

**v = \frac{1}{\paren{1 + n_{f}}}**  
<sub>theorem · the turn</sub>  

```
the choice is 1 way straight on against n_{f} folded
v = \frac{1}{\paren{1 + n_{f}}}
```

the turn draws one way straight on against the folded ways of the place, so the share of a step that carried a ray on is one against one plus the record

**shell \propto r^{\paren{D - 1}}**  
<sub>theorem · how many places are r steps out</sub>  

```
exits(p): the ways out of a point, DEG of them
steps: where one exit leads - and that is all the rules say about where anything is
D of those ways are independent, so places within r steps ∝ r^{D}
shell(r) = ball(r) - ball(r-1) ∝ r^{D-1}
```

the rules give a point, DEG ways out of it, and where each one leads - and nothing else about where anything is. So how far is how many steps were taken, and how many places are that far out is a count of walks. D is how many of those ways are independent, so within r steps there are r choices along each of D and the places within r go as r^{D}; the places at exactly r are the difference between two of those, which is r^{D-1}. Every step is whole and every place counted once, so this is the count itself and not an approximation to one

**l.DEG = \bar{DEG}**  
<sub>theorem · the ball and the shell it is bounded by</sub>  

```
l.DEG = l.shell(1) = \bar{DEG}
```

the ways out of a point, which is `l.shell` read at one - the discrete sphere of radius one

**l.shell\paren{\bar{R}} = \bar{R}^{\paren{D - 1}}**  
<sub>theorem · the ball and the shell it is bounded by</sub>  

```
l.shell(R) = \bar{R}^{\paren{D - 1}}
l.shell(1) = \bar{DEG} = DEG
```

the places at exactly R steps out, which `ehrhart` counts off the ways out of a point - and at R = 1 it is the ways out themselves, so DEG is this same count read at one rather than a number of its own

**l.ball\paren{\bar{R}}.count = \bar{R}^{D}**  
<sub>theorem · the ball and the shell it is bounded by</sub>  

```
l.ball(R).count = sum of l.shell up to R = \bar{R}^{D}
```

the places WITHIN R steps, which is the shell summed over every radius up to R - one power higher, by the same count of walks

**\rho = \text{the } \rho \text{ where } \bar{DEG} \paren{1 - \rho^{\bar{DEG}}} - 2 F \rho^{2} = 0**  
<sub>theorem · where the making pays for the taking</sub>  

```
\rho = \rho_{\infty} = \text{the } \rho \text{ where } \bar{DEG} \paren{1 - \rho^{\bar{DEG}}} - 2 F \rho^{2} = 0
```

and the density the line's own terms carry IS that settled one, everywhere the line is about the vacuum rather than about a source - so a law written in terms of the density can be written in terms of the rates instead

**\rho_{\infty} = \text{the } \rho \text{ where } \bar{DEG} \paren{1 - \rho^{\bar{DEG}}} - 2 F \rho^{2} = 0**  
<sub>theorem · where the making pays for the taking</sub>  

```
\nu \paren{1 - \rho^{\bar{DEG}}} = \sigma F \rho^{2}
rays made a firing: \bar{DEG},  rays taken a meeting: -2
\bar{DEG} \cdot \nu \paren{1 - \rho^{\bar{DEG}}} + -2 \cdot \sigma F \rho^{2} = 0
\rho_{\infty} = \text{the } \rho \text{ where } \bar{DEG} \paren{1 - \rho^{\bar{DEG}}} - 2 F \rho^{2} = 0
```

the vacuum settles where a neutral point's splitting exactly pays for what the meetings take. That is one equation in one unknown and it has one root that is not negative - so the density is FIXED by the rules rather than chosen, and it is the same on every lattice

**F = \frac{1}{2}**  
<sub>theorem · isotropy leaves no mean heading</sub>  

```
F = (1 - \hat{d} \cdot \hat{j}) / 2
\hat{j} = 0 where nothing is biased
F = 1/2
```

the facing factor is (1 - d^·j^)/2 and j^ is what the opposing population is doing on average. Alike in every direction, that is nothing - so a meeting in an undisturbed vacuum carries exactly a half, and the two limits it interpolates are one head-on and nought co-moving

**\lambda = \frac{1}{\paren{\sigma F \rho}}**  
<sub>theorem · one over the rate it is removed at</sub>  

```
removed at \sigma F \rho^{2} per \rho
\lambda = 1 / (\sigma F \rho) = \frac{1}{\paren{\sigma F \rho}}
```

what removes a ray is the meeting term, and a rate per unit of what is there is a rate per unit length once the density is settled. One over it is how far one carrier gets, which is the length everything else in this model is screened in

**\delta \text{ per site} = \frac{\paren{\delta \paren{1 + n_{f}}}}{\paren{r^{\paren{D - 1}}}}**  
<sub>theorem · spreading</sub>  

```
what crosses one site: δ/shell = \frac{\delta}{\paren{r^{\paren{D - 1}}}}
and it dwells 1/v there, with v = \frac{1}{\paren{1 + n_{f}}}
δ per site = \frac{\paren{\delta \paren{1 + n_{f}}}}{\paren{r^{\paren{D - 1}}}}
```

count what crosses a shell in one tick - the sites on it, times what is at each, times the share of a step that went outward - and MOVEMENT neither makes nor destroys, so that count is carried outward unchanged. So what CROSSES one site is the whole of it over the number of sites there are at that distance. What IS at one site is that again over how fast a share gets across

**\delta \text{ screened} = \frac{\paren{\delta \paren{1 + n_{f}}}}{\paren{r^{\paren{D - 1}}}}**  
<sub>theorem · what is pushed back is screened</sub>  

```
nothing restores δ, so survives(r) = 1
δ = \frac{\paren{\delta \paren{1 + n_{f}}}}{\paren{r^{\paren{D - 1}}}}
```

nothing in the line pushes δ back, so the chance of surviving one step is one and the chance of surviving r of them is one. What is left at r is what spread there, and the dilution is the whole of the law

**\text{the ambient field} = \infty**  
<sub>theorem · adding it up over every shell</sub>  

```
\sum_{r} shell(r) \cdot \delta(r) = \sum_{r} r^{\paren{D - 1}} \cdot \frac{\paren{\delta \paren{1 + n_{f}}}}{\paren{r^{\paren{D - 1}}}}
the two cancel: = \sum_{r} 1
which has no total
```

the r^{-(D-1)} in the falloff and the r^{D-1} in the shell cancel exactly, so every shell contributes alike and the sum over unboundedly many of them has no total. Nothing in the line pushes a discrepancy back, so there is no screening length to cut it off - which is Olbers' paradox standing, and it is what these rules give

**\text{S at the horizon} = \bar{DEG} \paren{r^{\paren{D - 1}}}**  
<sub>theorem · where the falloff meets the ceiling</sub>  

```
\delta per site = \frac{\paren{\delta \paren{1 + n_{f}}}}{\paren{r^{\paren{D - 1}}}}
\delta per site \le \bar{DEG}, the ways out a point has
S = \bar{DEG} \paren{r^{\paren{D - 1}}}
so on three dimensions a mass goes as the AREA of its horizon, not as its radius
```

the shortfall per site goes as S/r^{D-1} and cannot exceed DEG, the ways out a point has. Setting the two equal and solving for the SOURCE rather than for the radius gives the strength needed to put a horizon at r as the ceiling times the room out there

