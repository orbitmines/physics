# gravity.constant

> the assembled law carries SHEET^(2)/DEG in front of it, in either regime. What is that ratio, on this lattice?

Proved under `G`, `G^XOR`, `G^XOR*2`. Generated by `implementations/.ts/src/theorems` - nothing here was written by hand: every premise is a count or an exact invariance taken from the theory itself, and the conclusion is what the inference rules made of them. [Open the page](index.html) to switch theory and lattice.

| theory | answer |
|---|---|
| `G` | G = \frac{SHEET^(2)}{DEG} = 3 |
| `G^XOR` | G = \frac{SHEET^(2)}{DEG} = 3 |
| `G^XOR*2` | G = \frac{SHEET^(2)}{DEG} = 3 |

## G

### the result

**G = \frac{SHEET^(2)}{DEG} = 3**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 20 |

#### the derivation

**G = \frac{SHEET^(2)}{DEG}**  
<sub>definition · gravity.constant</sub>  

the assembled law carries SHEET^(2)/DEG in front of the masses and the inverse square - so the constant is those two counts, divided. Both came off the tiling, and there is nowhere in this line for a fitted number to be

**SHEET^(2) = SHEET · SHEET**  
<sub>definition · gravity.constant</sub>  

a meeting needs a charge from each side, so the pulse count enters twice

**SHEET = 6**  
<sub>premise · counts/what-the-tiling-fixes</sub>  

the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12. A count of the tiling, taken off the geometry itself with nothing run and nothing fitted

**SHEET^(2) = SHEET · SHEET = 36**  
<sub>derived · counts multiplied</sub>  

SHEET and SHEET are 6 and 6, so their product is 36

**DEG = 12**  
<sub>premise · counts/what-the-tiling-fixes</sub>  

the ways out of a point on fcc-12 - the exits it has, counted. A count of the tiling, taken off the geometry itself with nothing run and nothing fitted

**G = \frac{SHEET^(2)}{DEG} = 3**  
<sub>derived · a ratio of counts</sub>  

```
G = \frac{SHEET^(2)}{DEG}
= \frac{36}{12}
= 3
```

SHEET^(2) is 36 and DEG is 12, both counted off the tiling, so the ratio is 3 exactly - and it is worth reading as SHEET^(2)/DEG rather than as the number, because on another lattice it is a different number and the same ratio

#### what the runs found

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

## G^XOR

### the result

**G = \frac{SHEET^(2)}{DEG} = 3**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 20 |

#### the derivation

**G = \frac{SHEET^(2)}{DEG}**  
<sub>definition · gravity.constant</sub>  

the assembled law carries SHEET^(2)/DEG in front of the masses and the inverse square - so the constant is those two counts, divided. Both came off the tiling, and there is nowhere in this line for a fitted number to be

**SHEET^(2) = SHEET · SHEET**  
<sub>definition · gravity.constant</sub>  

a meeting needs a charge from each side, so the pulse count enters twice

**SHEET = 6**  
<sub>premise · counts/what-the-tiling-fixes</sub>  

the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12. A count of the tiling, taken off the geometry itself with nothing run and nothing fitted

**SHEET^(2) = SHEET · SHEET = 36**  
<sub>derived · counts multiplied</sub>  

SHEET and SHEET are 6 and 6, so their product is 36

**DEG = 12**  
<sub>premise · counts/what-the-tiling-fixes</sub>  

the ways out of a point on fcc-12 - the exits it has, counted. A count of the tiling, taken off the geometry itself with nothing run and nothing fitted

**G = \frac{SHEET^(2)}{DEG} = 3**  
<sub>derived · a ratio of counts</sub>  

```
G = \frac{SHEET^(2)}{DEG}
= \frac{36}{12}
= 3
```

SHEET^(2) is 36 and DEG is 12, both counted off the tiling, so the ratio is 3 exactly - and it is worth reading as SHEET^(2)/DEG rather than as the number, because on another lattice it is a different number and the same ratio

#### what the runs found

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

## G^XOR*2

### the result

**G = \frac{SHEET^(2)}{DEG} = 3**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 20 |

#### the derivation

**G = \frac{SHEET^(2)}{DEG}**  
<sub>definition · gravity.constant</sub>  

the assembled law carries SHEET^(2)/DEG in front of the masses and the inverse square - so the constant is those two counts, divided. Both came off the tiling, and there is nowhere in this line for a fitted number to be

**SHEET^(2) = SHEET · SHEET**  
<sub>definition · gravity.constant</sub>  

a meeting needs a charge from each side, so the pulse count enters twice

**SHEET = 6**  
<sub>premise · counts/what-the-tiling-fixes</sub>  

the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12. A count of the tiling, taken off the geometry itself with nothing run and nothing fitted

**SHEET^(2) = SHEET · SHEET = 36**  
<sub>derived · counts multiplied</sub>  

SHEET and SHEET are 6 and 6, so their product is 36

**DEG = 12**  
<sub>premise · counts/what-the-tiling-fixes</sub>  

the ways out of a point on fcc-12 - the exits it has, counted. A count of the tiling, taken off the geometry itself with nothing run and nothing fitted

**G = \frac{SHEET^(2)}{DEG} = 3**  
<sub>derived · a ratio of counts</sub>  

```
G = \frac{SHEET^(2)}{DEG}
= \frac{36}{12}
= 3
```

SHEET^(2) is 36 and DEG is 12, both counted off the tiling, so the ratio is 3 exactly - and it is worth reading as SHEET^(2)/DEG rather than as the number, because on another lattice it is a different number and the same ratio

#### what the runs found

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity
