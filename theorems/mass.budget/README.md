# mass.budget

> everything moves at one cell a tick, so a thing at rest is still moving. What is one step spent on, and what does that make conserved?

Proved under `G`, `G^XOR`, `G^XOR*2`, `G^XOR^c`, `G^XOR+XOR`, `G^XOR^o`, `G^XOR · relaxing(2/1)`. Generated by `implementations/.ts/src/theorems` - nothing here was written by hand: every premise is a count or an exact invariance taken from the theory itself, and the conclusion is what the inference rules made of them. [Open the page](index.html) to switch theory and lattice.

| theory | answer |
|---|---|
| `G` | no law follows |
| `G^XOR` | no law follows |
| `G^XOR*2` | \frac{(1-β^(2))·l.contained}{l.free} + m + β^(2) = 1 |
| `G^XOR^c` | \frac{(1-β^(2))·l.contained}{l.free} + m + β^(2) = 1 |
| `G^XOR+XOR` | no law follows |
| `G^XOR^o` | no law follows |
| `G^XOR · relaxing(2/1)` | no law follows |

## G

### the result

**no law follows for budget**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- |across|^(2) = m
- budget = m

#### the derivation

There isn't one - the rules could reach no law from what the probes found.

#### what the runs found

**`fcc-12`**

`budget/what-a-tick-is-spent-on` - holds. every exit of fcc-12 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - fcc-12 has one exit length: 1.414213562. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - fcc-12: 1.414213562. The first split is Pythagoras and needs one

**`cubic-6`**

`budget/what-a-tick-is-spent-on` - holds. every exit of cubic-6 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - cubic-6 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - cubic-6: 1.000000000. The first split is Pythagoras and needs one

**`square-4`**

`budget/what-a-tick-is-spent-on` - holds. every exit of square-4 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - square-4 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - square-4: 1.000000000. The first split is Pythagoras and needs one

**`line-2`**

`budget/what-a-tick-is-spent-on` - holds. every exit of line-2 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - line-2 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - line-2: 1.000000000. The first split is Pythagoras and needs one

## G^XOR

### the result

**no law follows for budget**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- |across|^(2) = m
- budget = m

#### the derivation

There isn't one - the rules could reach no law from what the probes found.

#### what the runs found

**`fcc-12`**

`budget/what-a-tick-is-spent-on` - holds. every exit of fcc-12 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - fcc-12 has one exit length: 1.414213562. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - fcc-12: 1.414213562. The first split is Pythagoras and needs one

**`cubic-6`**

`budget/what-a-tick-is-spent-on` - holds. every exit of cubic-6 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - cubic-6 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - cubic-6: 1.000000000. The first split is Pythagoras and needs one

**`square-4`**

`budget/what-a-tick-is-spent-on` - holds. every exit of square-4 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - square-4 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - square-4: 1.000000000. The first split is Pythagoras and needs one

**`line-2`**

`budget/what-a-tick-is-spent-on` - holds. every exit of line-2 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - line-2 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - line-2: 1.000000000. The first split is Pythagoras and needs one

## G^XOR*2

### the result

**\frac{(1-β^(2))·l.contained}{l.free} + m + β^(2) = 1**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**budget = |across|^(2) + β^(2)**  
<sub>premise · rest/what-a-thing-at-rest-is-doing</sub>  

and the two of them are one step. fcc-12 has a single exit length, so a step is a vector whose magnitude does not depend on which way it points - and orthogonal components of a fixed magnitude square to it

**β = β**  
<sub>premise · budget/what-a-tick-is-spent-on</sub>  

a ray bound into a moving structure has to keep pace with it or be left behind and cease to be part of it. So its step carries a component along the direction of travel equal to the structure's own speed - read off TRANSPORT, which moves the structure a cell at a time

**budget = |across|^(2) + β^(2)**  
<sub>derived · substituting</sub>  

```
budget = |across|^(2) + β^(2)
β = β
budget = |across|^(2) + β^(2)
```

spent is not a primitive of this theory - it is β, so it stands in for itself here and the result is multiplied out

**|across|^(2) = m + (1-β^(2))·\frac{l.contained}{l.free}**  
<sub>premise · rest/what-a-thing-at-rest-is-doing</sub>  

and what is left of the step after that divides again - LINEARLY. Every bend a ray makes happens at a point, and `blocks` sorts points into two kinds and no more: holding something, or not. That is a predicate, so the two are DISJOINT EVENTS partitioning an amount rather than orthogonal components resolving a vector. Amounts add; only vectors go in quadrature. Motion lost in the open is inertia and motion lost where matter is holding is what does the holding, and between them they are the whole of it

**m = (1-β^(2)) · (1 - shadow)**  
<sub>premise · rest/what-a-thing-at-rest-is-doing</sub>  

AND THE MASS IS WHAT THOSE TWO LEAVE. Not a definition of mass and not a measurement of one: the step is spent, the parts are named by which branch of which rule they fall on, and m is the one that is not held. MORE BINDING IS LESS MASS - the mass defect, and here it is the same subtraction rather than an analogy, because motion that has gone into holding a thing together is motion that is not available to weigh it

**shadow = \frac{l.contained}{l.free}**  
<sub>premise · rest/what-a-thing-at-rest-is-doing</sub>  

AND THE SHARE THAT DIVIDES THEM IS THE RULE ITSELF, not a number off a run. CREATION refuses the split at exactly those points where `blocks` says matter is held - and it reaches that test only where it has not already returned, which is `!l.source && !busy(l)`. So the share of the expansion that matter stops is one count over the other BY THE TEXT OF THE RULE. Nothing is fitted here and nothing needed to be run: the two counts are the two branches CREATION takes

**m = (1-β^(2)) - \frac{(1-β^(2))·l.contained}{l.free}**  
<sub>derived · substituting</sub>  

```
m = (1-β^(2)) - (1-β^(2))·shadow
shadow = \frac{l.contained}{l.free}
m = (1-β^(2)) - \frac{(1-β^(2))·l.contained}{l.free}
```

shadow is not a primitive of this theory - it is \frac{l.contained}{l.free}, so it stands in for itself here and the result is multiplied out

**|across|^(2) = (1-β^(2))**  
<sub>derived · substituting</sub>  

```
|across|^(2) = \frac{(1-β^(2))·l.contained}{l.free} + m
m = (1-β^(2)) - \frac{(1-β^(2))·l.contained}{l.free}
|across|^(2) = (1-β^(2))
```

m is not a primitive of this theory - it is (1-β^(2)) - \frac{(1-β^(2))·l.contained}{l.free}, so it stands in for itself here and the result is multiplied out

**(1-β^(2)) = |step|^(2) - |along|^(2)**  
<sub>premise · budget/what-a-tick-is-spent-on</sub>  

the step is ONE vector of fixed length, split into the part that keeps pace with the structure and the part that crosses it. Those are orthogonal, so their squares sum to the step's - which is Pythagoras on a lattice whose exits are all the same length, and not a claim about budgets. What is left to cross with is what advances the structure's own clock

**|step|^(2) = 1**  
<sub>premise · budget/what-a-tick-is-spent-on</sub>  

every exit of fcc-12 is the same length (1.414213562), so a ray's step is a vector whose magnitude does not depend on which way it points. Measured in steps that magnitude is one, which is what makes the next line Pythagoras rather than an analogy

**(1-β^(2)) = -|along|^(2) + 1**  
<sub>derived · substituting</sub>  

```
(1-β^(2)) = -|along|^(2) + |step|^(2)
|step|^(2) = 1
(1-β^(2)) = -|along|^(2) + 1
```

|step|^(2) is not a primitive of this theory - it is 1, so it stands in for itself here and the result is multiplied out

**|along|^(2) = β^(2)**  
<sub>premise · budget/what-a-tick-is-spent-on</sub>  

and the square of that component is what Pythagoras wants

**|along|^(2) = β^(2)**  
<sub>derived · substituting</sub>  

```
|along|^(2) = β^(2)
β = β
|along|^(2) = β^(2)
```

spent is not a primitive of this theory - it is β, so it stands in for itself here and the result is multiplied out

**(1-β^(2)) = 1 - β^(2)**  
<sub>derived · substituting</sub>  

```
(1-β^(2)) = -|along|^(2) + 1
|along|^(2) = β^(2)
(1-β^(2)) = 1 - β^(2)
```

|along|^(2) is not a primitive of this theory - it is β^(2), so it stands in for itself here and the result is multiplied out

**|across|^(2) = 1 - β^(2)**  
<sub>derived · substituting</sub>  

```
|across|^(2) = (1-β^(2))
(1-β^(2)) = 1 - β^(2)
|across|^(2) = 1 - β^(2)
```

(1-β^(2)) is not a primitive of this theory - it is 1 - β^(2), so it stands in for itself here and the result is multiplied out

**budget = 1**  
<sub>derived · substituting</sub>  

```
budget = |across|^(2) + β^(2)
|across|^(2) = 1 - β^(2)
budget = 1
```

|across|^(2) is not a primitive of this theory - it is 1 - β^(2), so it stands in for itself here and the result is multiplied out

**budget = 1**  
<sub>derived · which is a number</sub>  

1 has nothing left in it that varies, so it is 1 exactly

#### what the runs found

**`fcc-12`**

`budget/what-a-tick-is-spent-on` - holds. every exit of fcc-12 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR*2 declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - fcc-12 has one exit length: 1.414213562. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - holds. READ OFF THE RULES AND NOT OFF A RUN. A step of fixed length divides twice, and not the same way both times. MATTER carries every point of a structure together, so a bound ray keeps pace and its step has a component ALONG the motion; the rest crosses. Those are DIRECTIONS and square to the step - which is `budget`, and where the Lorentz factor comes from. Then CREATION sorts points with `blocks`, a predicate, into held and not-held: TWO ANSWERS, no direction between them, so that cut is a share and adds linearly. The share is the rule's own two branches - refused over reached - and the mass is what the other two leave. Nothing here was fitted and nothing was run

- a rule moves a whole structure at once = 1 - MATTER moves matter by RE-CONTAINING it - it takes what a point holds and folds it into the neighbour along one exit, and increments `moved` to say so. Everything that point held goes together, because containment is where matter IS. So a ray inside is CARRIED: that displacement is part of its step whatever the ray itself did
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - `blocks` takes 1 argument - a point - and answers yes or no. A predicate has no direction in it, so inside and outside are DISJOINT EVENTS and partition an amount. Amounts add linearly; vectors add in quadrature
- distinct exit lengths = 1 - fcc-12: 1.414213562. The first split is Pythagoras and needs one

**`cubic-6`**

`budget/what-a-tick-is-spent-on` - holds. every exit of cubic-6 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR*2 declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - cubic-6 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - holds. READ OFF THE RULES AND NOT OFF A RUN. A step of fixed length divides twice, and not the same way both times. MATTER carries every point of a structure together, so a bound ray keeps pace and its step has a component ALONG the motion; the rest crosses. Those are DIRECTIONS and square to the step - which is `budget`, and where the Lorentz factor comes from. Then CREATION sorts points with `blocks`, a predicate, into held and not-held: TWO ANSWERS, no direction between them, so that cut is a share and adds linearly. The share is the rule's own two branches - refused over reached - and the mass is what the other two leave. Nothing here was fitted and nothing was run

- a rule moves a whole structure at once = 1 - MATTER moves matter by RE-CONTAINING it - it takes what a point holds and folds it into the neighbour along one exit, and increments `moved` to say so. Everything that point held goes together, because containment is where matter IS. So a ray inside is CARRIED: that displacement is part of its step whatever the ray itself did
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - `blocks` takes 1 argument - a point - and answers yes or no. A predicate has no direction in it, so inside and outside are DISJOINT EVENTS and partition an amount. Amounts add linearly; vectors add in quadrature
- distinct exit lengths = 1 - cubic-6: 1.000000000. The first split is Pythagoras and needs one

**`square-4`**

`budget/what-a-tick-is-spent-on` - holds. every exit of square-4 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR*2 declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - square-4 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - holds. READ OFF THE RULES AND NOT OFF A RUN. A step of fixed length divides twice, and not the same way both times. MATTER carries every point of a structure together, so a bound ray keeps pace and its step has a component ALONG the motion; the rest crosses. Those are DIRECTIONS and square to the step - which is `budget`, and where the Lorentz factor comes from. Then CREATION sorts points with `blocks`, a predicate, into held and not-held: TWO ANSWERS, no direction between them, so that cut is a share and adds linearly. The share is the rule's own two branches - refused over reached - and the mass is what the other two leave. Nothing here was fitted and nothing was run

- a rule moves a whole structure at once = 1 - MATTER moves matter by RE-CONTAINING it - it takes what a point holds and folds it into the neighbour along one exit, and increments `moved` to say so. Everything that point held goes together, because containment is where matter IS. So a ray inside is CARRIED: that displacement is part of its step whatever the ray itself did
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - `blocks` takes 1 argument - a point - and answers yes or no. A predicate has no direction in it, so inside and outside are DISJOINT EVENTS and partition an amount. Amounts add linearly; vectors add in quadrature
- distinct exit lengths = 1 - square-4: 1.000000000. The first split is Pythagoras and needs one

**`line-2`**

`budget/what-a-tick-is-spent-on` - holds. every exit of line-2 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR*2 declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - line-2 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - holds. READ OFF THE RULES AND NOT OFF A RUN. A step of fixed length divides twice, and not the same way both times. MATTER carries every point of a structure together, so a bound ray keeps pace and its step has a component ALONG the motion; the rest crosses. Those are DIRECTIONS and square to the step - which is `budget`, and where the Lorentz factor comes from. Then CREATION sorts points with `blocks`, a predicate, into held and not-held: TWO ANSWERS, no direction between them, so that cut is a share and adds linearly. The share is the rule's own two branches - refused over reached - and the mass is what the other two leave. Nothing here was fitted and nothing was run

- a rule moves a whole structure at once = 1 - MATTER moves matter by RE-CONTAINING it - it takes what a point holds and folds it into the neighbour along one exit, and increments `moved` to say so. Everything that point held goes together, because containment is where matter IS. So a ray inside is CARRIED: that displacement is part of its step whatever the ray itself did
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - `blocks` takes 1 argument - a point - and answers yes or no. A predicate has no direction in it, so inside and outside are DISJOINT EVENTS and partition an amount. Amounts add linearly; vectors add in quadrature
- distinct exit lengths = 1 - line-2: 1.000000000. The first split is Pythagoras and needs one

## G^XOR^c

### the result

**\frac{(1-β^(2))·l.contained}{l.free} + m + β^(2) = 1**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**budget = |across|^(2) + β^(2)**  
<sub>premise · rest/what-a-thing-at-rest-is-doing</sub>  

and the two of them are one step. fcc-12 has a single exit length, so a step is a vector whose magnitude does not depend on which way it points - and orthogonal components of a fixed magnitude square to it

**β = β**  
<sub>premise · budget/what-a-tick-is-spent-on</sub>  

a ray bound into a moving structure has to keep pace with it or be left behind and cease to be part of it. So its step carries a component along the direction of travel equal to the structure's own speed - read off TRANSPORT, which moves the structure a cell at a time

**budget = |across|^(2) + β^(2)**  
<sub>derived · substituting</sub>  

```
budget = |across|^(2) + β^(2)
β = β
budget = |across|^(2) + β^(2)
```

spent is not a primitive of this theory - it is β, so it stands in for itself here and the result is multiplied out

**|across|^(2) = m + (1-β^(2))·\frac{l.contained}{l.free}**  
<sub>premise · rest/what-a-thing-at-rest-is-doing</sub>  

and what is left of the step after that divides again - LINEARLY. Every bend a ray makes happens at a point, and `blocks` sorts points into two kinds and no more: holding something, or not. That is a predicate, so the two are DISJOINT EVENTS partitioning an amount rather than orthogonal components resolving a vector. Amounts add; only vectors go in quadrature. Motion lost in the open is inertia and motion lost where matter is holding is what does the holding, and between them they are the whole of it

**m = (1-β^(2)) · (1 - shadow)**  
<sub>premise · rest/what-a-thing-at-rest-is-doing</sub>  

AND THE MASS IS WHAT THOSE TWO LEAVE. Not a definition of mass and not a measurement of one: the step is spent, the parts are named by which branch of which rule they fall on, and m is the one that is not held. MORE BINDING IS LESS MASS - the mass defect, and here it is the same subtraction rather than an analogy, because motion that has gone into holding a thing together is motion that is not available to weigh it

**shadow = \frac{l.contained}{l.free}**  
<sub>premise · rest/what-a-thing-at-rest-is-doing</sub>  

AND THE SHARE THAT DIVIDES THEM IS THE RULE ITSELF, not a number off a run. CREATION refuses the split at exactly those points where `blocks` says matter is held - and it reaches that test only where it has not already returned, which is `!l.source && !busy(l)`. So the share of the expansion that matter stops is one count over the other BY THE TEXT OF THE RULE. Nothing is fitted here and nothing needed to be run: the two counts are the two branches CREATION takes

**m = (1-β^(2)) - \frac{(1-β^(2))·l.contained}{l.free}**  
<sub>derived · substituting</sub>  

```
m = (1-β^(2)) - (1-β^(2))·shadow
shadow = \frac{l.contained}{l.free}
m = (1-β^(2)) - \frac{(1-β^(2))·l.contained}{l.free}
```

shadow is not a primitive of this theory - it is \frac{l.contained}{l.free}, so it stands in for itself here and the result is multiplied out

**|across|^(2) = (1-β^(2))**  
<sub>derived · substituting</sub>  

```
|across|^(2) = \frac{(1-β^(2))·l.contained}{l.free} + m
m = (1-β^(2)) - \frac{(1-β^(2))·l.contained}{l.free}
|across|^(2) = (1-β^(2))
```

m is not a primitive of this theory - it is (1-β^(2)) - \frac{(1-β^(2))·l.contained}{l.free}, so it stands in for itself here and the result is multiplied out

**(1-β^(2)) = |step|^(2) - |along|^(2)**  
<sub>premise · budget/what-a-tick-is-spent-on</sub>  

the step is ONE vector of fixed length, split into the part that keeps pace with the structure and the part that crosses it. Those are orthogonal, so their squares sum to the step's - which is Pythagoras on a lattice whose exits are all the same length, and not a claim about budgets. What is left to cross with is what advances the structure's own clock

**|step|^(2) = 1**  
<sub>premise · budget/what-a-tick-is-spent-on</sub>  

every exit of fcc-12 is the same length (1.414213562), so a ray's step is a vector whose magnitude does not depend on which way it points. Measured in steps that magnitude is one, which is what makes the next line Pythagoras rather than an analogy

**(1-β^(2)) = -|along|^(2) + 1**  
<sub>derived · substituting</sub>  

```
(1-β^(2)) = -|along|^(2) + |step|^(2)
|step|^(2) = 1
(1-β^(2)) = -|along|^(2) + 1
```

|step|^(2) is not a primitive of this theory - it is 1, so it stands in for itself here and the result is multiplied out

**|along|^(2) = β^(2)**  
<sub>premise · budget/what-a-tick-is-spent-on</sub>  

and the square of that component is what Pythagoras wants

**|along|^(2) = β^(2)**  
<sub>derived · substituting</sub>  

```
|along|^(2) = β^(2)
β = β
|along|^(2) = β^(2)
```

spent is not a primitive of this theory - it is β, so it stands in for itself here and the result is multiplied out

**(1-β^(2)) = 1 - β^(2)**  
<sub>derived · substituting</sub>  

```
(1-β^(2)) = -|along|^(2) + 1
|along|^(2) = β^(2)
(1-β^(2)) = 1 - β^(2)
```

|along|^(2) is not a primitive of this theory - it is β^(2), so it stands in for itself here and the result is multiplied out

**|across|^(2) = 1 - β^(2)**  
<sub>derived · substituting</sub>  

```
|across|^(2) = (1-β^(2))
(1-β^(2)) = 1 - β^(2)
|across|^(2) = 1 - β^(2)
```

(1-β^(2)) is not a primitive of this theory - it is 1 - β^(2), so it stands in for itself here and the result is multiplied out

**budget = 1**  
<sub>derived · substituting</sub>  

```
budget = |across|^(2) + β^(2)
|across|^(2) = 1 - β^(2)
budget = 1
```

|across|^(2) is not a primitive of this theory - it is 1 - β^(2), so it stands in for itself here and the result is multiplied out

**budget = 1**  
<sub>derived · which is a number</sub>  

1 has nothing left in it that varies, so it is 1 exactly

#### what the runs found

**`fcc-12`**

`budget/what-a-tick-is-spent-on` - holds. every exit of fcc-12 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR^c declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - fcc-12 has one exit length: 1.414213562. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - holds. READ OFF THE RULES AND NOT OFF A RUN. A step of fixed length divides twice, and not the same way both times. MOVING carries every point of a structure together, so a bound ray keeps pace and its step has a component ALONG the motion; the rest crosses. Those are DIRECTIONS and square to the step - which is `budget`, and where the Lorentz factor comes from. Then CREATION sorts points with `blocks`, a predicate, into held and not-held: TWO ANSWERS, no direction between them, so that cut is a share and adds linearly. The share is the rule's own two branches - refused over reached - and the mass is what the other two leave. Nothing here was fitted and nothing was run

- a rule moves a whole structure at once = 1 - MOVING moves matter by RE-CONTAINING it - it takes what a point holds and folds it into the neighbour along one exit, and increments `moved` to say so. Everything that point held goes together, because containment is where matter IS. So a ray inside is CARRIED: that displacement is part of its step whatever the ray itself did
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - `blocks` takes 1 argument - a point - and answers yes or no. A predicate has no direction in it, so inside and outside are DISJOINT EVENTS and partition an amount. Amounts add linearly; vectors add in quadrature
- distinct exit lengths = 1 - fcc-12: 1.414213562. The first split is Pythagoras and needs one

**`cubic-6`**

`budget/what-a-tick-is-spent-on` - holds. every exit of cubic-6 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR^c declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - cubic-6 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - holds. READ OFF THE RULES AND NOT OFF A RUN. A step of fixed length divides twice, and not the same way both times. MOVING carries every point of a structure together, so a bound ray keeps pace and its step has a component ALONG the motion; the rest crosses. Those are DIRECTIONS and square to the step - which is `budget`, and where the Lorentz factor comes from. Then CREATION sorts points with `blocks`, a predicate, into held and not-held: TWO ANSWERS, no direction between them, so that cut is a share and adds linearly. The share is the rule's own two branches - refused over reached - and the mass is what the other two leave. Nothing here was fitted and nothing was run

- a rule moves a whole structure at once = 1 - MOVING moves matter by RE-CONTAINING it - it takes what a point holds and folds it into the neighbour along one exit, and increments `moved` to say so. Everything that point held goes together, because containment is where matter IS. So a ray inside is CARRIED: that displacement is part of its step whatever the ray itself did
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - `blocks` takes 1 argument - a point - and answers yes or no. A predicate has no direction in it, so inside and outside are DISJOINT EVENTS and partition an amount. Amounts add linearly; vectors add in quadrature
- distinct exit lengths = 1 - cubic-6: 1.000000000. The first split is Pythagoras and needs one

**`square-4`**

`budget/what-a-tick-is-spent-on` - holds. every exit of square-4 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR^c declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - square-4 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - holds. READ OFF THE RULES AND NOT OFF A RUN. A step of fixed length divides twice, and not the same way both times. MOVING carries every point of a structure together, so a bound ray keeps pace and its step has a component ALONG the motion; the rest crosses. Those are DIRECTIONS and square to the step - which is `budget`, and where the Lorentz factor comes from. Then CREATION sorts points with `blocks`, a predicate, into held and not-held: TWO ANSWERS, no direction between them, so that cut is a share and adds linearly. The share is the rule's own two branches - refused over reached - and the mass is what the other two leave. Nothing here was fitted and nothing was run

- a rule moves a whole structure at once = 1 - MOVING moves matter by RE-CONTAINING it - it takes what a point holds and folds it into the neighbour along one exit, and increments `moved` to say so. Everything that point held goes together, because containment is where matter IS. So a ray inside is CARRIED: that displacement is part of its step whatever the ray itself did
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - `blocks` takes 1 argument - a point - and answers yes or no. A predicate has no direction in it, so inside and outside are DISJOINT EVENTS and partition an amount. Amounts add linearly; vectors add in quadrature
- distinct exit lengths = 1 - square-4: 1.000000000. The first split is Pythagoras and needs one

**`line-2`**

`budget/what-a-tick-is-spent-on` - holds. every exit of line-2 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR^c declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - line-2 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - holds. READ OFF THE RULES AND NOT OFF A RUN. A step of fixed length divides twice, and not the same way both times. MOVING carries every point of a structure together, so a bound ray keeps pace and its step has a component ALONG the motion; the rest crosses. Those are DIRECTIONS and square to the step - which is `budget`, and where the Lorentz factor comes from. Then CREATION sorts points with `blocks`, a predicate, into held and not-held: TWO ANSWERS, no direction between them, so that cut is a share and adds linearly. The share is the rule's own two branches - refused over reached - and the mass is what the other two leave. Nothing here was fitted and nothing was run

- a rule moves a whole structure at once = 1 - MOVING moves matter by RE-CONTAINING it - it takes what a point holds and folds it into the neighbour along one exit, and increments `moved` to say so. Everything that point held goes together, because containment is where matter IS. So a ray inside is CARRIED: that displacement is part of its step whatever the ray itself did
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - `blocks` takes 1 argument - a point - and answers yes or no. A predicate has no direction in it, so inside and outside are DISJOINT EVENTS and partition an amount. Amounts add linearly; vectors add in quadrature
- distinct exit lengths = 1 - line-2: 1.000000000. The first split is Pythagoras and needs one

## G^XOR+XOR

### the result

**no law follows for budget**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- |across|^(2) = m
- budget = m

#### the derivation

There isn't one - the rules could reach no law from what the probes found.

#### what the runs found

**`fcc-12`**

`budget/what-a-tick-is-spent-on` - holds. every exit of fcc-12 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR+XOR declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - fcc-12 has one exit length: 1.414213562. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR+XOR moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - fcc-12: 1.414213562. The first split is Pythagoras and needs one

**`cubic-6`**

`budget/what-a-tick-is-spent-on` - holds. every exit of cubic-6 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR+XOR declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - cubic-6 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR+XOR moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - cubic-6: 1.000000000. The first split is Pythagoras and needs one

**`square-4`**

`budget/what-a-tick-is-spent-on` - holds. every exit of square-4 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR+XOR declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - square-4 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR+XOR moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - square-4: 1.000000000. The first split is Pythagoras and needs one

**`line-2`**

`budget/what-a-tick-is-spent-on` - holds. every exit of line-2 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR+XOR declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - line-2 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR+XOR moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - line-2: 1.000000000. The first split is Pythagoras and needs one

## G^XOR^o

### the result

**no law follows for budget**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- |across|^(2) = m
- budget = m

#### the derivation

There isn't one - the rules could reach no law from what the probes found.

#### what the runs found

**`fcc-12`**

`budget/what-a-tick-is-spent-on` - holds. every exit of fcc-12 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR^o declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - fcc-12 has one exit length: 1.414213562. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR^o moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - fcc-12: 1.414213562. The first split is Pythagoras and needs one

**`cubic-6`**

`budget/what-a-tick-is-spent-on` - holds. every exit of cubic-6 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR^o declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - cubic-6 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR^o moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - cubic-6: 1.000000000. The first split is Pythagoras and needs one

**`square-4`**

`budget/what-a-tick-is-spent-on` - holds. every exit of square-4 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR^o declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - square-4 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR^o moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - square-4: 1.000000000. The first split is Pythagoras and needs one

**`line-2`**

`budget/what-a-tick-is-spent-on` - holds. every exit of line-2 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR^o declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - line-2 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR^o moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 1 - `busy(l)` is read at character 93 and `blocks` at 133, so a point already carrying a ray RETURNS BEFORE the blocks test and never refuses a split however much matter it holds. The shadow is therefore over the points that reach the test, not over all of them - and reading it over all of them gives an answer twice too big
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - line-2: 1.000000000. The first split is Pythagoras and needs one

## G^XOR · relaxing(2/1)

### the result

**no law follows for budget**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

**What was missing.** The rules needed these and no probe established them here:

- |across|^(2) = m
- budget = m

#### the derivation

There isn't one - the rules could reach no law from what the probes found.

#### what the runs found

**`fcc-12`**

`budget/what-a-tick-is-spent-on` - holds. every exit of fcc-12 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR · relaxing(2/1) declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - fcc-12 has one exit length: 1.414213562. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR · relaxing(2/1) moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - fcc-12: 1.414213562. The first split is Pythagoras and needs one

**`cubic-6`**

`budget/what-a-tick-is-spent-on` - holds. every exit of cubic-6 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR · relaxing(2/1) declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - cubic-6 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR · relaxing(2/1) moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - cubic-6: 1.000000000. The first split is Pythagoras and needs one

**`square-4`**

`budget/what-a-tick-is-spent-on` - holds. every exit of square-4 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR · relaxing(2/1) declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - square-4 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR · relaxing(2/1) moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - square-4: 1.000000000. The first split is Pythagoras and needs one

**`line-2`**

`budget/what-a-tick-is-spent-on` - holds. every exit of line-2 is the same length, so a ray's step is a vector of fixed magnitude. A ray bound into a structure moving at β spends β of that step keeping pace, and what is left to cross with - which is the clock - is sqrt(1 - β^(2)) by Pythagoras. The Lorentz factor, derived from the step rather than assumed

- rules that move a structure = 1 - TRANSPORT, over "World" - and it takes the upkeep before it moves anything, which is what "not both" means
- the theory carries an upkeep = 1 - G^XOR · relaxing(2/1) declares `upkeep` - what one period of a structure's own clock costs
- distinct exit lengths = 1 - line-2 has one exit length: 1.000000000. A step is a vector of fixed magnitude only where there is one

`rest/what-a-thing-at-rest-is-doing` - does not hold. G^XOR · relaxing(2/1) moves no structure as a whole, so its rules do not separate motion that holds a thing together from motion that weighs it. The split has no referent and the probe declines rather than dividing by a distinction that is not there

- a rule moves a whole structure at once = 0 - nothing here moves emergent matter as a whole. TRANSPORT moves a declared source's own cells, which is a region of the lattice and not something rays are inside of, so no ray is carried and nothing gives one a component to keep
- (G/2) is refused where matter is held = 1 - CREATION consults `blocks`, and the split it then does not make is the expansion that did not happen - which is the gravity of this model
- and it asks `busy` first = 0 - nothing here gates the blocks test, so every point holding matter refuses
- directions the inside/outside split names = 0 - this theory has no `blocks` to ask
- distinct exit lengths = 1 - line-2: 1.000000000. The first split is Pythagoras and needs one
