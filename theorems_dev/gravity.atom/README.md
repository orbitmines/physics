# gravity.atom

> a source emits gravity, and so does anything that turns. With no counting condition of any kind, what relation does that fix between a bound thing's mass and its size?

Proved under `G`, `G^XOR`, `G^XOR*2`, `G^XOR^c`, `G^XOR+XOR`, `G^XOR^o`, `G^XOR · relaxing(2/1)`. Generated by `implementations/.ts/src/theorems` - nothing here was written by hand: every premise is a count or an exact invariance taken from the theory itself, and the conclusion is what the inference rules made of them. [Open the page](index.html) to switch theory and lattice.

| theory | answer |
|---|---|
| `G` | m_cloud → ∞ |
| `G^XOR` | m_cloud → ∞ |
| `G^XOR*2` | m_cloud → ∞ |
| `G^XOR^c` | m_cloud → ∞ |
| `G^XOR+XOR` | m_cloud → ∞ |
| `G^XOR^o` | m_cloud → ∞ |
| `G^XOR · relaxing(2/1)` | m_cloud → ∞ |

## G

### the result

**m_cloud → ∞**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**m_cloud = \sum_r M_r = M_r · L**  
<sub>definition · gravity.atom</sub>  

AND THE CLOUD IS ALL OF THOSE SHELLS ADDED UP. Since every shell weighs the same, the sum is that weight times HOW MANY SHELLS THERE ARE - so what the cloud comes to is set by HOW FAR IT REACHES and not by how the field falls off. That extent is what an energy state IS here: how fast the thing rotates and how far what it throws off gets before it is eaten. The mass ratio is therefore a question about L, and the falloff has cancelled itself out of it

**M_r = \omega_r · shell**  
<sub>definition · gravity.atom</sub>  

SO A WHOLE SHELL IS WHAT ONE CELL DOES TIMES HOW MANY CELLS THERE ARE, and this is where the two exponents meet: the field thins as r^(1-D) and the room grows as r^(D-1), so THE PRODUCT DOES NOT DEPEND ON r AT ALL. Every shell of the cloud weighs the same, which is conservation said as an arithmetic - the rays that left the source are all still there, spread thinner over more room

**\omega_r ∝ g**  
<sub>definition · gravity.atom</sub>  

what one cell out there is doing is what the source's gravity comes to there, which is the first line again - a cell is turned as hard as the field at it

**shell = STEP·r^(D-1)**  
<sub>definition · gravity.atom</sub>  

AND THE VACUUM AROUND IT IS MANY MANY CELLS relative to the thing at the middle, which is the whole point: `lattice.shell-growth` counts the room at a distance as STEP·r^(D-1), and that count GROWS while the field thins

**M_r ∝ 1**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**m_cloud → ∞**  
<sub>derived · summing over every shell</sub>  

```
m_cloud = \sum_r^(∞) M_r
M_r ∝ 1
\sum_r^(∞) r^(0) → ∞
```

M_r goes as 1, so in r it falls off as r^(0) - which is to say it does not fall off at all. Every shell contributes the same, and there is no end of shells. So the total does not converge

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

## G^XOR

### the result

**m_cloud → ∞**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**m_cloud = \sum_r M_r = M_r · L**  
<sub>definition · gravity.atom</sub>  

AND THE CLOUD IS ALL OF THOSE SHELLS ADDED UP. Since every shell weighs the same, the sum is that weight times HOW MANY SHELLS THERE ARE - so what the cloud comes to is set by HOW FAR IT REACHES and not by how the field falls off. That extent is what an energy state IS here: how fast the thing rotates and how far what it throws off gets before it is eaten. The mass ratio is therefore a question about L, and the falloff has cancelled itself out of it

**M_r = \omega_r · shell**  
<sub>definition · gravity.atom</sub>  

SO A WHOLE SHELL IS WHAT ONE CELL DOES TIMES HOW MANY CELLS THERE ARE, and this is where the two exponents meet: the field thins as r^(1-D) and the room grows as r^(D-1), so THE PRODUCT DOES NOT DEPEND ON r AT ALL. Every shell of the cloud weighs the same, which is conservation said as an arithmetic - the rays that left the source are all still there, spread thinner over more room

**\omega_r ∝ g**  
<sub>definition · gravity.atom</sub>  

what one cell out there is doing is what the source's gravity comes to there, which is the first line again - a cell is turned as hard as the field at it

**shell = STEP·r^(D-1)**  
<sub>definition · gravity.atom</sub>  

AND THE VACUUM AROUND IT IS MANY MANY CELLS relative to the thing at the middle, which is the whole point: `lattice.shell-growth` counts the room at a distance as STEP·r^(D-1), and that count GROWS while the field thins

**M_r ∝ 1**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**m_cloud → ∞**  
<sub>derived · summing over every shell</sub>  

```
m_cloud = \sum_r^(∞) M_r
M_r ∝ 1
\sum_r^(∞) r^(0) → ∞
```

M_r goes as 1, so in r it falls off as r^(0) - which is to say it does not fall off at all. Every shell contributes the same, and there is no end of shells. So the total does not converge

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

## G^XOR*2

### the result

**m_cloud → ∞**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**m_cloud = \sum_r M_r = M_r · L**  
<sub>definition · gravity.atom</sub>  

AND THE CLOUD IS ALL OF THOSE SHELLS ADDED UP. Since every shell weighs the same, the sum is that weight times HOW MANY SHELLS THERE ARE - so what the cloud comes to is set by HOW FAR IT REACHES and not by how the field falls off. That extent is what an energy state IS here: how fast the thing rotates and how far what it throws off gets before it is eaten. The mass ratio is therefore a question about L, and the falloff has cancelled itself out of it

**M_r = \omega_r · shell**  
<sub>definition · gravity.atom</sub>  

SO A WHOLE SHELL IS WHAT ONE CELL DOES TIMES HOW MANY CELLS THERE ARE, and this is where the two exponents meet: the field thins as r^(1-D) and the room grows as r^(D-1), so THE PRODUCT DOES NOT DEPEND ON r AT ALL. Every shell of the cloud weighs the same, which is conservation said as an arithmetic - the rays that left the source are all still there, spread thinner over more room

**\omega_r ∝ g**  
<sub>definition · gravity.atom</sub>  

what one cell out there is doing is what the source's gravity comes to there, which is the first line again - a cell is turned as hard as the field at it

**shell = STEP·r^(D-1)**  
<sub>definition · gravity.atom</sub>  

AND THE VACUUM AROUND IT IS MANY MANY CELLS relative to the thing at the middle, which is the whole point: `lattice.shell-growth` counts the room at a distance as STEP·r^(D-1), and that count GROWS while the field thins

**M_r ∝ 1**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**m_cloud → ∞**  
<sub>derived · summing over every shell</sub>  

```
m_cloud = \sum_r^(∞) M_r
M_r ∝ 1
\sum_r^(∞) r^(0) → ∞
```

M_r goes as 1, so in r it falls off as r^(0) - which is to say it does not fall off at all. Every shell contributes the same, and there is no end of shells. So the total does not converge

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

## G^XOR^c

### the result

**m_cloud → ∞**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**m_cloud = \sum_r M_r = M_r · L**  
<sub>definition · gravity.atom</sub>  

AND THE CLOUD IS ALL OF THOSE SHELLS ADDED UP. Since every shell weighs the same, the sum is that weight times HOW MANY SHELLS THERE ARE - so what the cloud comes to is set by HOW FAR IT REACHES and not by how the field falls off. That extent is what an energy state IS here: how fast the thing rotates and how far what it throws off gets before it is eaten. The mass ratio is therefore a question about L, and the falloff has cancelled itself out of it

**M_r = \omega_r · shell**  
<sub>definition · gravity.atom</sub>  

SO A WHOLE SHELL IS WHAT ONE CELL DOES TIMES HOW MANY CELLS THERE ARE, and this is where the two exponents meet: the field thins as r^(1-D) and the room grows as r^(D-1), so THE PRODUCT DOES NOT DEPEND ON r AT ALL. Every shell of the cloud weighs the same, which is conservation said as an arithmetic - the rays that left the source are all still there, spread thinner over more room

**\omega_r ∝ g**  
<sub>definition · gravity.atom</sub>  

what one cell out there is doing is what the source's gravity comes to there, which is the first line again - a cell is turned as hard as the field at it

**shell = STEP·r^(D-1)**  
<sub>definition · gravity.atom</sub>  

AND THE VACUUM AROUND IT IS MANY MANY CELLS relative to the thing at the middle, which is the whole point: `lattice.shell-growth` counts the room at a distance as STEP·r^(D-1), and that count GROWS while the field thins

**M_r ∝ 1**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**m_cloud → ∞**  
<sub>derived · summing over every shell</sub>  

```
m_cloud = \sum_r^(∞) M_r
M_r ∝ 1
\sum_r^(∞) r^(0) → ∞
```

M_r goes as 1, so in r it falls off as r^(0) - which is to say it does not fall off at all. Every shell contributes the same, and there is no end of shells. So the total does not converge

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

## G^XOR+XOR

### the result

**m_cloud → ∞**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**m_cloud = \sum_r M_r = M_r · L**  
<sub>definition · gravity.atom</sub>  

AND THE CLOUD IS ALL OF THOSE SHELLS ADDED UP. Since every shell weighs the same, the sum is that weight times HOW MANY SHELLS THERE ARE - so what the cloud comes to is set by HOW FAR IT REACHES and not by how the field falls off. That extent is what an energy state IS here: how fast the thing rotates and how far what it throws off gets before it is eaten. The mass ratio is therefore a question about L, and the falloff has cancelled itself out of it

**M_r = \omega_r · shell**  
<sub>definition · gravity.atom</sub>  

SO A WHOLE SHELL IS WHAT ONE CELL DOES TIMES HOW MANY CELLS THERE ARE, and this is where the two exponents meet: the field thins as r^(1-D) and the room grows as r^(D-1), so THE PRODUCT DOES NOT DEPEND ON r AT ALL. Every shell of the cloud weighs the same, which is conservation said as an arithmetic - the rays that left the source are all still there, spread thinner over more room

**\omega_r ∝ g**  
<sub>definition · gravity.atom</sub>  

what one cell out there is doing is what the source's gravity comes to there, which is the first line again - a cell is turned as hard as the field at it

**shell = STEP·r^(D-1)**  
<sub>definition · gravity.atom</sub>  

AND THE VACUUM AROUND IT IS MANY MANY CELLS relative to the thing at the middle, which is the whole point: `lattice.shell-growth` counts the room at a distance as STEP·r^(D-1), and that count GROWS while the field thins

**M_r ∝ 1**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**m_cloud → ∞**  
<sub>derived · summing over every shell</sub>  

```
m_cloud = \sum_r^(∞) M_r
M_r ∝ 1
\sum_r^(∞) r^(0) → ∞
```

M_r goes as 1, so in r it falls off as r^(0) - which is to say it does not fall off at all. Every shell contributes the same, and there is no end of shells. So the total does not converge

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

## G^XOR^o

### the result

**m_cloud → ∞**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**m_cloud = \sum_r M_r = M_r · L**  
<sub>definition · gravity.atom</sub>  

AND THE CLOUD IS ALL OF THOSE SHELLS ADDED UP. Since every shell weighs the same, the sum is that weight times HOW MANY SHELLS THERE ARE - so what the cloud comes to is set by HOW FAR IT REACHES and not by how the field falls off. That extent is what an energy state IS here: how fast the thing rotates and how far what it throws off gets before it is eaten. The mass ratio is therefore a question about L, and the falloff has cancelled itself out of it

**M_r = \omega_r · shell**  
<sub>definition · gravity.atom</sub>  

SO A WHOLE SHELL IS WHAT ONE CELL DOES TIMES HOW MANY CELLS THERE ARE, and this is where the two exponents meet: the field thins as r^(1-D) and the room grows as r^(D-1), so THE PRODUCT DOES NOT DEPEND ON r AT ALL. Every shell of the cloud weighs the same, which is conservation said as an arithmetic - the rays that left the source are all still there, spread thinner over more room

**\omega_r ∝ g**  
<sub>definition · gravity.atom</sub>  

what one cell out there is doing is what the source's gravity comes to there, which is the first line again - a cell is turned as hard as the field at it

**shell = STEP·r^(D-1)**  
<sub>definition · gravity.atom</sub>  

AND THE VACUUM AROUND IT IS MANY MANY CELLS relative to the thing at the middle, which is the whole point: `lattice.shell-growth` counts the room at a distance as STEP·r^(D-1), and that count GROWS while the field thins

**M_r ∝ 1**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**m_cloud → ∞**  
<sub>derived · summing over every shell</sub>  

```
m_cloud = \sum_r^(∞) M_r
M_r ∝ 1
\sum_r^(∞) r^(0) → ∞
```

M_r goes as 1, so in r it falls off as r^(0) - which is to say it does not fall off at all. Every shell contributes the same, and there is no end of shells. So the total does not converge

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

## G^XOR · relaxing(2/1)

### the result

**m_cloud → ∞**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**m_cloud = \sum_r M_r = M_r · L**  
<sub>definition · gravity.atom</sub>  

AND THE CLOUD IS ALL OF THOSE SHELLS ADDED UP. Since every shell weighs the same, the sum is that weight times HOW MANY SHELLS THERE ARE - so what the cloud comes to is set by HOW FAR IT REACHES and not by how the field falls off. That extent is what an energy state IS here: how fast the thing rotates and how far what it throws off gets before it is eaten. The mass ratio is therefore a question about L, and the falloff has cancelled itself out of it

**M_r = \omega_r · shell**  
<sub>definition · gravity.atom</sub>  

SO A WHOLE SHELL IS WHAT ONE CELL DOES TIMES HOW MANY CELLS THERE ARE, and this is where the two exponents meet: the field thins as r^(1-D) and the room grows as r^(D-1), so THE PRODUCT DOES NOT DEPEND ON r AT ALL. Every shell of the cloud weighs the same, which is conservation said as an arithmetic - the rays that left the source are all still there, spread thinner over more room

**\omega_r ∝ g**  
<sub>definition · gravity.atom</sub>  

what one cell out there is doing is what the source's gravity comes to there, which is the first line again - a cell is turned as hard as the field at it

**shell = STEP·r^(D-1)**  
<sub>definition · gravity.atom</sub>  

AND THE VACUUM AROUND IT IS MANY MANY CELLS relative to the thing at the middle, which is the whole point: `lattice.shell-growth` counts the room at a distance as STEP·r^(D-1), and that count GROWS while the field thins

**M_r ∝ 1**  
<sub>derived · multiplying</sub>  

each factor carries its own dependence and they multiply

**m_cloud → ∞**  
<sub>derived · summing over every shell</sub>  

```
m_cloud = \sum_r^(∞) M_r
M_r ∝ 1
\sum_r^(∞) r^(0) → ∞
```

M_r goes as 1, so in r it falls off as r^(0) - which is to say it does not fall off at all. Every shell contributes the same, and there is no end of shells. So the total does not converge

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity
