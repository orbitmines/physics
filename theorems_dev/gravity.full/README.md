# gravity.full

> put the pieces together. What is the gravitational force between two bodies R apart, with every factor written in?

Proved under `G`, `G^XOR`, `G^XOR*2`, `G^XOR^c`, `G^XOR+XOR`, `G^XOR^o`, `G^XOR · relaxing(2/1)`. Generated by `implementations/.ts/src/theorems` - nothing here was written by hand: every premise is a count or an exact invariance taken from the theory itself, and the conclusion is what the inference rules made of them. [Open the page](index.html) to switch theory and lattice.

| theory | answer |
|---|---|
| `G` | F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{2·\frac{1}{c̄^(D-3)} + \frac{c̄·ln(R/c̄)}{R}}} (and 1 other result) |
| `G^XOR` | F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}} (and 1 other result) |
| `G^XOR*2` | F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}} (and 1 other result) |
| `G^XOR^c` | F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}} (and 1 other result) |
| `G^XOR+XOR` | F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}} (and 1 other result) |
| `G^XOR^o` | F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}} (and 1 other result) |
| `G^XOR · relaxing(2/1)` | F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}} (and 1 other result) |

## G

### result 1 of 2

**F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{2·\frac{1}{c̄^(D-3)} + \frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^(-D+1)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the dense regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^(D-1)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^(D-1)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + 2·\frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + 2·\frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^(D)} + 2·\frac{SHEET^(2)·m_a·m_b}{DEG·R^(D-1)·c̄^(D-3)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

### result 2 of 2

**F_g = \frac{1}{R^((D-1)/2)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{2·\frac{1}{c̄^((D-5)/2)} + \frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^((-D+1)/2)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the thin regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^((D-1)/2)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^((D-1)/2)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + 2·\frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + 2·\frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^((D+1)/2)} + 2·\frac{SHEET^(2)·m_a·m_b}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

## G^XOR

### result 1 of 2

**F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^(-D+1)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the dense regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^(D-1)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^(D-1)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^(D)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^(D-1)·c̄^(D-3)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

### result 2 of 2

**F_g = \frac{1}{R^((D-1)/2)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^((D-5)/2)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^((-D+1)/2)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the thin regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^((D-1)/2)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^((D-1)/2)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

## G^XOR*2

### result 1 of 2

**F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^(-D+1)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the dense regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^(D-1)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^(D-1)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^(D)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^(D-1)·c̄^(D-3)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

### result 2 of 2

**F_g = \frac{1}{R^((D-1)/2)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^((D-5)/2)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^((-D+1)/2)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the thin regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^((D-1)/2)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^((D-1)/2)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

## G^XOR^c

### result 1 of 2

**F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^(-D+1)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the dense regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^(D-1)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^(D-1)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^(D)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^(D-1)·c̄^(D-3)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

### result 2 of 2

**F_g = \frac{1}{R^((D-1)/2)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^((D-5)/2)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^((-D+1)/2)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the thin regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^((D-1)/2)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^((D-1)/2)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

## G^XOR+XOR

### result 1 of 2

**F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^(-D+1)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the dense regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^(D-1)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^(D-1)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^(D)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^(D-1)·c̄^(D-3)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

### result 2 of 2

**F_g = \frac{1}{R^((D-1)/2)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^((D-5)/2)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^((-D+1)/2)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the thin regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^((D-1)/2)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^((D-1)/2)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

## G^XOR^o

### result 1 of 2

**F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^(-D+1)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the dense regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^(D-1)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^(D-1)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^(D)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^(D-1)·c̄^(D-3)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

### result 2 of 2

**F_g = \frac{1}{R^((D-1)/2)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^((D-5)/2)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^((-D+1)/2)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the thin regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^((D-1)/2)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^((D-1)/2)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

## G^XOR · relaxing(2/1)

### result 1 of 2

**F_g = \frac{1}{R^(D-1)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^(D-3)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^(-D+1)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the dense regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^(D-1)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^(D-1)·STEP}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^(D-1)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^(D)} + \frac{SHEET^(2)·m·m'}{DEG·R^(D-1)·c̄^(D-3)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^(D-1)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^(D-1)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^(D)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^(D-1)·c̄^(D-3)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

### result 2 of 2

**F_g = \frac{1}{R^((D-1)/2)}·\paren{\frac{A'·A_\perp}{STEP} + \frac{SHEET^(2)·m·m'}{DEG}·\paren{\frac{1}{c̄^((D-5)/2)} + 1/2·\frac{c̄·ln(R/c̄)}{R}}}**

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**F_g = F_meet + F_vac**  
<sub>definition · gravity.full</sub>  

what a body feels is everything that arrives at it, and things that arrive add. So the gravitational force is the two channels together: the meetings between the bodies' own radiation, and the expansion the vacuum did not manage around them

**F_meet = lean · share · SHEET^(2) · m · m' · met(R)**  
<sub>definition · gravity.full</sub>  

what arrives through MEETINGS is what a meeting is worth to a path, times how much of the time the two phases are opposed, times the charges each pulse lets go on both sides, times the two masses, times the meetings summed along the line between them. Every one of those was proved somewhere else and is cited; this line only says how they multiply

**F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_meet + F_vac
F_meet = SHEET^(2)·lean·m·m'·met(R)·share
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
```

F_meet is not a primitive of this theory - it is SHEET^(2)·lean·m_a·m_b·met(R)·share, so it stands in for itself here and the result is multiplied out

**F_vac = \frac{A_\perp · S}{STEP}·R^((-D+1)/2)**  
<sub>definition · gravity.full</sub>  

and what arrives because the vacuum's expansion was SUPPRESSED is the other channel. A body's cells are not neutral, so the split does not fire on them; the expansion that did not happen there spreads outward and a second body is pushed into the shortfall. This needs neither body to emit anything - an inert absorber has it - which is what makes it a separate arrival rather than the meeting term counted again. It thins as the thin regime says, because the deficit is carried by the same rays as everything else. Note the two areas are not the same kind of area: what the near body SENDS leaves through the whole of its boundary and spreads isotropically, so its full A' counts; what the far body FEELS is an imbalance along the line between them, so only its facing cross-section takes part

**S = A'**  
<sub>cited · vacuum.suppression</sub>  

established earlier by vacuum.suppression, on this same theory and lattice - the working is there rather than repeated here

**F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}**  
<sub>derived · substituting</sub>  

```
F_vac = \frac{A_\perp·S}{R^((D-1)/2)·STEP}
S = A'
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
```

S is not a primitive of this theory - it is A', so it stands in for itself here and the result is multiplied out

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share**  
<sub>derived · substituting</sub>  

```
F_g = F_vac + SHEET^(2)·lean·m·m'·met(R)·share
F_vac = \frac{A'·A_\perp}{R^((D-1)/2)·STEP}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
```

F_vac is not a primitive of this theory - it is \frac{A'·A_\perp}{R^((D-1)/2)·STEP}, so it stands in for itself here and the result is multiplied out

**lean = \frac{c̄}{DEG}**  
<sub>cited · lattice.lean</sub>  

established earlier by lattice.lean, on this same theory and lattice - the working is there rather than repeated here

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}**  
<sub>derived · substituting</sub>  

```
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + SHEET^(2)·lean·m·m'·met(R)·share
lean = \frac{c̄}{DEG}
F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + \frac{SHEET^(2)·c̄·m·m'·met(R)·share}{DEG}
```

lean is not a primitive of this theory - it is \frac{c̄}{DEG}, so it stands in for itself here and the result is multiplied out

**F_g = \frac{num(F_g)}{den(F_g)}**  
<sub>derived · over a common denominator</sub>  

so F_g is the one over the other

**num(F_g) = \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share**  
<sub>derived · over a common denominator</sub>  

putting F_g over DEG·STEP, what is left on top is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share - the schoolbook move, and nothing about it knows what these quantities are

**den(F_g) = DEG·STEP**  
<sub>derived · over a common denominator</sub>  

and underneath is DEG·STEP

**F_g = \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}**  
<sub>derived · one over another</sub>  

```
F_g = num(F_g) / den(F_g)
= (\frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m·m'·met(R)·share) / (DEG·STEP)
= \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m·m'}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m·m'}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}
```

num(F_g) is \frac{A'·A_\perp·DEG}{R^((D-1)/2)} + SHEET^(2)·STEP·c̄·m_a·m_b·met(R)·share and den(F_g) is DEG·STEP, so the one over the other is \frac{A'·A_\perp}{R^((D-1)/2)·STEP} + 1/2·\frac{SHEET^(2)·c̄·ln(R/c̄)·m_a·m_b}{DEG·R^((D+1)/2)} + \frac{SHEET^(2)·m_a·m_b}{DEG·R^((D-1)/2)·c̄^((D-5)/2)}

#### what the runs found

**`fcc-12`**

`counts/what-the-tiling-fixes` - holds. fcc-12: D = 3, DEG = 12, SHEET = 6, CYCLE = 6 - counts, not measurements

- D = 3 - how many dimensions fcc-12 has
- DEG = 12 - the ways out of a point on fcc-12 - the exits it has, counted
- SHEET = 6 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 12
- CYCLE = 6 - how many steps go round the ring of a turn on fcc-12
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for fcc-12, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 13, 55, 147, 309, 561, 923, 1415, 2057 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 20, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 13, 55, 147, 309, 561, 923, 1415, 2057  |  Δ^(1) = 12, 42, 92, 162, 252, 362, 492, 642  |  Δ^(2) = 30, 50, 70, 90, 110, 130, 150  |  Δ^(3) = 20, 20, 20, 20, 20, 20
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 12 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`cubic-6`**

`counts/what-the-tiling-fixes` - holds. cubic-6: D = 3, DEG = 6, SHEET = 4, CYCLE = 4 - counts, not measurements

- D = 3 - how many dimensions cubic-6 has
- DEG = 6 - the ways out of a point on cubic-6 - the exits it has, counted
- SHEET = 4 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 6
- CYCLE = 4 - how many steps go round the ring of a turn on cubic-6
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 3 in r̄ (D is 3), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for cubic-6, by elimination in 3 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 7, 25, 63, 129, 231, 377, 575, 833 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 3 - the 3-th difference of the counts is constant at 8, and a sequence whose 3-th difference is constant is a polynomial of degree 3. Integer arithmetic - ball(r̄) = 1, 7, 25, 63, 129, 231, 377, 575, 833  |  Δ^(1) = 6, 18, 38, 66, 102, 146, 198, 258  |  Δ^(2) = 12, 20, 28, 36, 44, 52, 60  |  Δ^(3) = 8, 8, 8, 8, 8, 8
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 6 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`square-4`**

`counts/what-the-tiling-fixes` - holds. square-4: D = 2, DEG = 4, SHEET = 2, CYCLE = 4 - counts, not measurements

- D = 2 - how many dimensions square-4 has
- DEG = 4 - the ways out of a point on square-4 - the exits it has, counted
- SHEET = 2 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 4
- CYCLE = 4 - how many steps go round the ring of a turn on square-4
- slice = 2 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 2 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 2 in r̄ (D is 2), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is isotropic at second order

- volume of one fundamental cell = 1 - |det basis| for square-4, by elimination in 2 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 5, 13, 25, 41, 61, 85, 113, 145 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 2 - the 2-th difference of the counts is constant at 4, and a sequence whose 2-th difference is constant is a polynomial of degree 2. Integer arithmetic - ball(r̄) = 1, 5, 13, 25, 41, 61, 85, 113, 145  |  Δ^(1) = 4, 8, 12, 16, 20, 24, 28, 32  |  Δ^(2) = 4, 4, 4, 4, 4, 4, 4
- second moment anisotropy = 0.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 4 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled

**`line-2`**

`counts/what-the-tiling-fixes` - holds. line-2: D = 1, DEG = 2, SHEET = 0, CYCLE = 0 - counts, not measurements

- D = 1 - how many dimensions line-2 has
- DEG = 2 - the ways out of a point on line-2 - the exits it has, counted
- SHEET = 0 - the exits with no component along the pulsing axis, which is what a source emits over. NOT DEG, which is 2
- CYCLE = 0 - how many steps go round the ring of a turn on line-2
- slice = 1 - a turn sweeps a PLANE, and a plane needs two dimensions - so what a turn lives in is min(D, 2), which is 1 here and stops growing at 2 however many dimensions are added
- c̄ = 1 - a step's worth of lean is what one annihilation buys a path, and a step is c̄ - one cell a tick, the lattice's own speed. Its value in those units is one, which is a statement about the units and not about the quantity

`lattice/what-the-tiling-is` - holds. the ball is a polynomial of degree 1 in r̄ (D is 1), checked exactly by finite differences; ρ = 1.0000 per unit volume; the exit set is anisotropic at 2.0000 at second order

- volume of one fundamental cell = 1 - |det basis| for line-2, by elimination in 1 dimensions - from the basis vectors themselves, with no run and no fit
- sites within r̄ steps = 8 - walked over the lattice's own exits from the centre: 1, 3, 5, 7, 9, 11, 13, 15, 17 for r̄ = 0, 1, 2, ...
- degree of the count in r̄ = 1 - the 1-th difference of the counts is constant at 2, and a sequence whose 1-th difference is constant is a polynomial of degree 1. Integer arithmetic - ball(r̄) = 1, 3, 5, 7, 9, 11, 13, 15, 17  |  Δ^(1) = 2, 2, 2, 2, 2, 2, 2, 2
- second moment anisotropy = 2.0000 - the spread of Σw·(c·p)^(2) over directions p, computed from all 2 exit vectors - 0 is perfectly even. Exhaustive over the exit set, so it holds for all time and every configuration rather than being sampled
