# gravity.horizon

> the shortfall around a body grows without limit as you approach it, and a point has only so many exits to be missing. What happens where they meet?

Proved under `G`, `G^XOR`, `G^XOR*2`, `G^XOR^c`, `G^XOR+XOR`, `G^XOR^o`, `G^XOR · relaxing(2/1)`. Generated by `implementations/.ts/src/theorems` - nothing here was written by hand: every premise is a count or an exact invariance taken from the theory itself, and the conclusion is what the inference rules made of them. [Open the page](index.html) to switch theory and lattice.

| theory | answer |
|---|---|
| `G` | what gets out = 0 |
| `G^XOR` | what gets out = 0 |
| `G^XOR*2` | what gets out = 0 |
| `G^XOR^c` | what gets out = 0 |
| `G^XOR+XOR` | what gets out = 0 |
| `G^XOR^o` | what gets out = 0 |
| `G^XOR · relaxing(2/1)` | what gets out = 0 |

## G

### the result

**what gets out = 0**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**what gets out = 0**  
<sub>premise · saturation/what-a-point-cannot-lose-more-than</sub>  

at the ceiling every one of a point's 12 exits is dark. Leaving is what an exit is, so nothing leaves such a point by any route - not a weakened signal, none. That is a horizon in the only sense this model has one, and it is not put in anywhere: it is where a law that has to keep growing meets a count that cannot

#### what the runs found

**`fcc-12`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on fcc-12 has 12 exits, so the shortfall at one point is at most 12 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 12 - the ways out of a point on fcc-12, counted. A shortfall at a point is exits that are dark, and there are 12 of them, so the shortfall at one point can be at most 12. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`cubic-6`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on cubic-6 has 6 exits, so the shortfall at one point is at most 6 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 6 - the ways out of a point on cubic-6, counted. A shortfall at a point is exits that are dark, and there are 6 of them, so the shortfall at one point can be at most 6. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`square-4`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on square-4 has 4 exits, so the shortfall at one point is at most 4 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 4 - the ways out of a point on square-4, counted. A shortfall at a point is exits that are dark, and there are 4 of them, so the shortfall at one point can be at most 4. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`line-2`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on line-2 has 2 exits, so the shortfall at one point is at most 2 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 2 - the ways out of a point on line-2, counted. A shortfall at a point is exits that are dark, and there are 2 of them, so the shortfall at one point can be at most 2. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

## G^XOR

### the result

**what gets out = 0**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**what gets out = 0**  
<sub>premise · saturation/what-a-point-cannot-lose-more-than</sub>  

at the ceiling every one of a point's 12 exits is dark. Leaving is what an exit is, so nothing leaves such a point by any route - not a weakened signal, none. That is a horizon in the only sense this model has one, and it is not put in anywhere: it is where a law that has to keep growing meets a count that cannot

#### what the runs found

**`fcc-12`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on fcc-12 has 12 exits, so the shortfall at one point is at most 12 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 12 - the ways out of a point on fcc-12, counted. A shortfall at a point is exits that are dark, and there are 12 of them, so the shortfall at one point can be at most 12. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`cubic-6`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on cubic-6 has 6 exits, so the shortfall at one point is at most 6 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 6 - the ways out of a point on cubic-6, counted. A shortfall at a point is exits that are dark, and there are 6 of them, so the shortfall at one point can be at most 6. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`square-4`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on square-4 has 4 exits, so the shortfall at one point is at most 4 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 4 - the ways out of a point on square-4, counted. A shortfall at a point is exits that are dark, and there are 4 of them, so the shortfall at one point can be at most 4. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`line-2`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on line-2 has 2 exits, so the shortfall at one point is at most 2 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 2 - the ways out of a point on line-2, counted. A shortfall at a point is exits that are dark, and there are 2 of them, so the shortfall at one point can be at most 2. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

## G^XOR*2

### the result

**what gets out = 0**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**what gets out = 0**  
<sub>premise · saturation/what-a-point-cannot-lose-more-than</sub>  

at the ceiling every one of a point's 12 exits is dark. Leaving is what an exit is, so nothing leaves such a point by any route - not a weakened signal, none. That is a horizon in the only sense this model has one, and it is not put in anywhere: it is where a law that has to keep growing meets a count that cannot

#### what the runs found

**`fcc-12`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on fcc-12 has 12 exits, so the shortfall at one point is at most 12 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 12 - the ways out of a point on fcc-12, counted. A shortfall at a point is exits that are dark, and there are 12 of them, so the shortfall at one point can be at most 12. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`cubic-6`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on cubic-6 has 6 exits, so the shortfall at one point is at most 6 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 6 - the ways out of a point on cubic-6, counted. A shortfall at a point is exits that are dark, and there are 6 of them, so the shortfall at one point can be at most 6. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`square-4`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on square-4 has 4 exits, so the shortfall at one point is at most 4 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 4 - the ways out of a point on square-4, counted. A shortfall at a point is exits that are dark, and there are 4 of them, so the shortfall at one point can be at most 4. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`line-2`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on line-2 has 2 exits, so the shortfall at one point is at most 2 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 2 - the ways out of a point on line-2, counted. A shortfall at a point is exits that are dark, and there are 2 of them, so the shortfall at one point can be at most 2. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

## G^XOR^c

### the result

**what gets out = 0**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**what gets out = 0**  
<sub>premise · saturation/what-a-point-cannot-lose-more-than</sub>  

at the ceiling every one of a point's 12 exits is dark. Leaving is what an exit is, so nothing leaves such a point by any route - not a weakened signal, none. That is a horizon in the only sense this model has one, and it is not put in anywhere: it is where a law that has to keep growing meets a count that cannot

#### what the runs found

**`fcc-12`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on fcc-12 has 12 exits, so the shortfall at one point is at most 12 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 12 - the ways out of a point on fcc-12, counted. A shortfall at a point is exits that are dark, and there are 12 of them, so the shortfall at one point can be at most 12. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`cubic-6`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on cubic-6 has 6 exits, so the shortfall at one point is at most 6 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 6 - the ways out of a point on cubic-6, counted. A shortfall at a point is exits that are dark, and there are 6 of them, so the shortfall at one point can be at most 6. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`square-4`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on square-4 has 4 exits, so the shortfall at one point is at most 4 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 4 - the ways out of a point on square-4, counted. A shortfall at a point is exits that are dark, and there are 4 of them, so the shortfall at one point can be at most 4. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`line-2`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on line-2 has 2 exits, so the shortfall at one point is at most 2 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 2 - the ways out of a point on line-2, counted. A shortfall at a point is exits that are dark, and there are 2 of them, so the shortfall at one point can be at most 2. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

## G^XOR+XOR

### the result

**what gets out = 0**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**what gets out = 0**  
<sub>premise · saturation/what-a-point-cannot-lose-more-than</sub>  

at the ceiling every one of a point's 12 exits is dark. Leaving is what an exit is, so nothing leaves such a point by any route - not a weakened signal, none. That is a horizon in the only sense this model has one, and it is not put in anywhere: it is where a law that has to keep growing meets a count that cannot

#### what the runs found

**`fcc-12`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on fcc-12 has 12 exits, so the shortfall at one point is at most 12 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 12 - the ways out of a point on fcc-12, counted. A shortfall at a point is exits that are dark, and there are 12 of them, so the shortfall at one point can be at most 12. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`cubic-6`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on cubic-6 has 6 exits, so the shortfall at one point is at most 6 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 6 - the ways out of a point on cubic-6, counted. A shortfall at a point is exits that are dark, and there are 6 of them, so the shortfall at one point can be at most 6. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`square-4`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on square-4 has 4 exits, so the shortfall at one point is at most 4 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 4 - the ways out of a point on square-4, counted. A shortfall at a point is exits that are dark, and there are 4 of them, so the shortfall at one point can be at most 4. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`line-2`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on line-2 has 2 exits, so the shortfall at one point is at most 2 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 2 - the ways out of a point on line-2, counted. A shortfall at a point is exits that are dark, and there are 2 of them, so the shortfall at one point can be at most 2. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

## G^XOR^o

### the result

**what gets out = 0**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**what gets out = 0**  
<sub>premise · saturation/what-a-point-cannot-lose-more-than</sub>  

at the ceiling every one of a point's 12 exits is dark. Leaving is what an exit is, so nothing leaves such a point by any route - not a weakened signal, none. That is a horizon in the only sense this model has one, and it is not put in anywhere: it is where a law that has to keep growing meets a count that cannot

#### what the runs found

**`fcc-12`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on fcc-12 has 12 exits, so the shortfall at one point is at most 12 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 12 - the ways out of a point on fcc-12, counted. A shortfall at a point is exits that are dark, and there are 12 of them, so the shortfall at one point can be at most 12. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`cubic-6`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on cubic-6 has 6 exits, so the shortfall at one point is at most 6 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 6 - the ways out of a point on cubic-6, counted. A shortfall at a point is exits that are dark, and there are 6 of them, so the shortfall at one point can be at most 6. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`square-4`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on square-4 has 4 exits, so the shortfall at one point is at most 4 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 4 - the ways out of a point on square-4, counted. A shortfall at a point is exits that are dark, and there are 4 of them, so the shortfall at one point can be at most 4. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`line-2`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on line-2 has 2 exits, so the shortfall at one point is at most 2 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 2 - the ways out of a point on line-2, counted. A shortfall at a point is exits that are dark, and there are 2 of them, so the shortfall at one point can be at most 2. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

## G^XOR · relaxing(2/1)

### the result

**what gets out = 0**

The law holds, but nothing established that it is about a quantity greater than zero.

| lattice | D | DEG | box | ticks |
|---|---|---|---|---|
| `fcc-12` | 3 | 12 | 21 | 120 |
| `cubic-6` | 3 | 6 | 21 | 120 |
| `square-4` | 2 | 4 | 41 | 120 |
| `line-2` | 1 | 2 | 61 | 120 |

#### the derivation

**what gets out = 0**  
<sub>premise · saturation/what-a-point-cannot-lose-more-than</sub>  

at the ceiling every one of a point's 12 exits is dark. Leaving is what an exit is, so nothing leaves such a point by any route - not a weakened signal, none. That is a horizon in the only sense this model has one, and it is not put in anywhere: it is where a law that has to keep growing meets a count that cannot

#### what the runs found

**`fcc-12`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on fcc-12 has 12 exits, so the shortfall at one point is at most 12 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 12 - the ways out of a point on fcc-12, counted. A shortfall at a point is exits that are dark, and there are 12 of them, so the shortfall at one point can be at most 12. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`cubic-6`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on cubic-6 has 6 exits, so the shortfall at one point is at most 6 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 6 - the ways out of a point on cubic-6, counted. A shortfall at a point is exits that are dark, and there are 6 of them, so the shortfall at one point can be at most 6. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`square-4`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on square-4 has 4 exits, so the shortfall at one point is at most 4 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 4 - the ways out of a point on square-4, counted. A shortfall at a point is exits that are dark, and there are 4 of them, so the shortfall at one point can be at most 4. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise

**`line-2`**

`saturation/what-a-point-cannot-lose-more-than` - holds. a point on line-2 has 2 exits, so the shortfall at one point is at most 2 - and where the falloff reaches it, every exit is dark and nothing leaves at all. The horizon is where an unbounded law meets the only ceiling the arithmetic already had

- DEG = 2 - the ways out of a point on line-2, counted. A shortfall at a point is exits that are dark, and there are 2 of them, so the shortfall at one point can be at most 2. Read off the tiling with nothing ticked, and there is no way for it to come out otherwise
