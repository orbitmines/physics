// GENERATED by `npx ray gen` from implementation/physics.ray - do not edit
import { test } from "node:test";
import assert from "node:assert/strict";
import * as physics from "../../languages/physics.ts/index.ts";
const { G, World, Vertex, Ray, Boundary, Edge, Source, Geometry, Vector, Random, Rule, Theory, Field, Hole, Bodies, Around, Cell, Beam, Port, Rates, Grid, Solve, Piece, Reference, Setter, Notation, Surface, Measured, Recording, Played, Painter, Still, Pane, Picture, Fmt, Probe, Trial, Orbit, Setup, Body, Panel, Ink, Side, Lane, Strip, Sparc, Law, Galaxies, Annulus, Simulation, Formed, Model, Sweep, Measure, Aggregate, Medium, LINE2, SQUARE4, SQUARE8, CUBIC6, FCC12, CUBIC18, CUBIC26, BCC8, eq, lt, le, gt, ge, add, sub, mul, div, mod, neg, elem, first, last, sum, most, range, filled, many, contains, index_of, instance_of, sorted_by } = physics;

test("tests/coincidence.ray:13 on coincidence", (t) => {
  const it = new Model({ theory: G });
  let guard = 0;
  while (!(eq(it.theory.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(Fmt.finite(it.value_of(`\\frac{a_{0}}{cH}`, it.settled(8))), "Fmt.finite(it.value_of(`\\\\frac{a_{0}}{cH}`, it.settled(8)))");
});

test("tests/coincidence.ray:14 on coincidence", (t) => {
  const it = new Model({ theory: G });
  let guard = 0;
  while (!(eq(it.theory.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.value_of(`\\frac{a_{0}}{cH}`, it.settled(8)), 0), "gt(it.value_of(`\\\\frac{a_{0}}{cH}`, it.settled(8)), 0)");
});

test("tests/coincidence.ray:16 on coincidence", (t) => {
  const it = new Model({ theory: G });
  let guard = 0;
  while (!(eq(it.theory.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.value_of(`\\frac{a_{0}}{cH}`, it.settled(6)), it.value_of(`\\frac{a_{0}}{cH}`, it.settled(8))), "gt(it.value_of(`\\\\frac{a_{0}}{cH}`, it.settled(6)), it.value_of(`\\\\frac{a_{0}}{cH}`, it.settled(8)))");
});

test("tests/coincidence.ray:17 on coincidence", (t) => {
  const it = new Model({ theory: G });
  let guard = 0;
  while (!(eq(it.theory.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.value_of(`\\frac{a_{0}}{cH}`, it.settled(8)), it.value_of(`\\frac{a_{0}}{cH}`, it.settled(18))), "gt(it.value_of(`\\\\frac{a_{0}}{cH}`, it.settled(8)), it.value_of(`\\\\frac{a_{0}}{cH}`, it.settled(18)))");
});

test("tests/coincidence.ray:19 on coincidence", (t) => {
  const it = new Model({ theory: G });
  let guard = 0;
  while (!(eq(it.theory.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(mul(it.value_of(`\\frac{a_{0}}{cH}`, it.settled(8)), Math.sqrt((sub(1, 0.315)))), 0.155), "gt(mul(it.value_of(`\\\\frac{a_{0}}{cH}`, it.settled(8)), Math.sqrt((sub(1, 0.315)))), 0.155)");
});

test("tests/coincidence.ray:20 on coincidence", (t) => {
  const it = new Model({ theory: G });
  let guard = 0;
  while (!(eq(it.theory.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(mul(it.value_of(`\\frac{a_{0}}{cH}`, it.settled(8)), Math.sqrt((sub(1, 0.315)))), 0.183), "lt(mul(it.value_of(`\\\\frac{a_{0}}{cH}`, it.settled(8)), Math.sqrt((sub(1, 0.315)))), 0.183)");
});

test("tests/field.ray:10 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.n.every(((v: any) => {
  return ge(v, 0);
})), "it.n.every(((v: any) => {\n  return ge(v, 0);\n}))");
});

test("tests/field.ray:10 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.n.every(((v: any) => {
  return ge(v, 0);
})), "it.n.every(((v: any) => {\n  return ge(v, 0);\n}))");
});

test("tests/field.ray:10 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.n.every(((v: any) => {
  return ge(v, 0);
})), "it.n.every(((v: any) => {\n  return ge(v, 0);\n}))");
});

test("tests/field.ray:10 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.n.every(((v: any) => {
  return ge(v, 0);
})), "it.n.every(((v: any) => {\n  return ge(v, 0);\n}))");
});

test("tests/field.ray:11 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.mean, 0), "gt(it.mean, 0)");
});

test("tests/field.ray:11 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.mean, 0), "gt(it.mean, 0)");
});

test("tests/field.ray:11 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.mean, 0), "gt(it.mean, 0)");
});

test("tests/field.ray:11 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.mean, 0), "gt(it.mean, 0)");
});

test("tests/field.ray:12 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.rho.every(((v: any) => {
  return ge(v, 0);
})), "it.rho.every(((v: any) => {\n  return ge(v, 0);\n}))");
});

test("tests/field.ray:12 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.rho.every(((v: any) => {
  return ge(v, 0);
})), "it.rho.every(((v: any) => {\n  return ge(v, 0);\n}))");
});

test("tests/field.ray:12 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.rho.every(((v: any) => {
  return ge(v, 0);
})), "it.rho.every(((v: any) => {\n  return ge(v, 0);\n}))");
});

test("tests/field.ray:12 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!(eq(it.ticks, 5))) { if (++guard > 7 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.rho.every(((v: any) => {
  return ge(v, 0);
})), "it.rho.every(((v: any) => {\n  return ge(v, 0);\n}))");
});

test("tests/field.ray:17 on vacuum", (t) => {
  const it = new Rates({ nu: 1, sigma: 1, F: 0.5, DEG: 8, D: 2 });
  let guard = 0;
  while (!(eq(it.nu, 1))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(Math.abs((sub(Solve.settles(it), 0.7320508075688772))), 0.000001), "lt(Math.abs((sub(Solve.settles(it), 0.7320508075688772))), 0.000001)");
});

test("tests/field.ray:18 on vacuum", (t) => {
  const it = new Rates({ nu: 1, sigma: 1, F: 0.5, DEG: 8, D: 2 });
  let guard = 0;
  while (!(eq(it.nu, 1))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(Solve.settles(it), 0), "gt(Solve.settles(it), 0)");
});

test("tests/field.ray:19 on vacuum", (t) => {
  const it = new Rates({ nu: 1, sigma: 1, F: 0.5, DEG: 8, D: 2 });
  let guard = 0;
  while (!(eq(it.nu, 1))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(Solve.settles(it), 1), "lt(Solve.settles(it), 1)");
});

test("tests/galaxy.ray:10 on ring", (t) => {
  const it = new Annulus({ inner: 4, outer: 6, thick: 0.2 });
  let guard = 0;
  while (!(eq(it.outer, 6))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(Math.abs((sub(mul(mul(it.pull(200, 4, 64), 200), 200), 1))), 0.002), "lt(Math.abs((sub(mul(mul(it.pull(200, 4, 64), 200), 200), 1))), 0.002)");
});

test("tests/galaxy.ray:11 on ring", (t) => {
  const it = new Annulus({ inner: 4, outer: 6, thick: 0.2 });
  let guard = 0;
  while (!(eq(it.outer, 6))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(Math.abs(it.pull(0.01, 4, 64)), 0.001), "lt(Math.abs(it.pull(0.01, 4, 64)), 0.001)");
});

test("tests/galaxy.ray:13 on ring", (t) => {
  const it = new Annulus({ inner: 4, outer: 6, thick: 0.2 });
  let guard = 0;
  while (!(eq(it.outer, 6))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(mul(it.pull(7, 4, 64), 49), 1), "gt(mul(it.pull(7, 4, 64), 49), 1)");
});

test("tests/galaxy.ray:14 on ring", (t) => {
  const it = new Annulus({ inner: 4, outer: 6, thick: 0.2 });
  let guard = 0;
  while (!(eq(it.outer, 6))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(it.pull(3, 4, 64), 0), "lt(it.pull(3, 4, 64), 0)");
});

test("tests/medium.ray:11 on law", (t) => {
  const it = Aggregate.of(G);
  let guard = 0;
  while (!(eq(it.DEG, 8))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.rho_inf, 0), "gt(it.rho_inf, 0)");
});

test("tests/medium.ray:12 on law", (t) => {
  const it = Aggregate.of(G);
  let guard = 0;
  while (!(eq(it.DEG, 8))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(it.rho_inf, 1), "lt(it.rho_inf, 1)");
});

test("tests/medium.ray:13 on law", (t) => {
  const it = Aggregate.of(G);
  let guard = 0;
  while (!(eq(it.DEG, 8))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(Fmt.finite(it.nf_inf), "Fmt.finite(it.nf_inf)");
});

test("tests/medium.ray:14 on law", (t) => {
  const it = Aggregate.of(G);
  let guard = 0;
  while (!(eq(it.DEG, 8))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(!eq(it.step(`record`), null), "!eq(it.step(`record`), null)");
});

test("tests/medium.ray:15 on law", (t) => {
  const it = Aggregate.of(G);
  let guard = 0;
  while (!(eq(it.DEG, 8))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(!eq(it.step(`line`), null), "!eq(it.step(`line`), null)");
});

test("tests/medium.ray:16 on law", (t) => {
  const it = Aggregate.of(G);
  let guard = 0;
  while (!(eq(it.DEG, 8))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok([1, 2, 4, 8, 16, 32].every(((R: any) => {
  return Fmt.finite(it.pull(1, R));
})), "[1, 2, 4, 8, 16, 32].every(((R: any) => {\n  return Fmt.finite(it.pull(1, R));\n}))");
});

test("tests/medium.ray:18 on law", (t) => {
  const it = Aggregate.of(G);
  let guard = 0;
  while (!(eq(it.DEG, 8))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(Fmt.finite(it.circling(1, 12, 6)), "Fmt.finite(it.circling(1, 12, 6))");
});

test("tests/medium.ray:19 on law", (t) => {
  const it = Aggregate.of(G);
  let guard = 0;
  while (!(eq(it.DEG, 8))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(it.circling(1, 12, 6), 1), "lt(it.circling(1, 12, 6), 1)");
});

test("tests/medium.ray:27 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).x, 7), "eq(elem(it.holes, 0).x, 7)");
});

test("tests/medium.ray:27 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).x, 7), "eq(elem(it.holes, 0).x, 7)");
});

test("tests/medium.ray:27 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).x, 7), "eq(elem(it.holes, 0).x, 7)");
});

test("tests/medium.ray:28 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).y, 7), "eq(elem(it.holes, 0).y, 7)");
});

test("tests/medium.ray:28 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).y, 7), "eq(elem(it.holes, 0).y, 7)");
});

test("tests/medium.ray:28 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).y, 7), "eq(elem(it.holes, 0).y, 7)");
});

test("tests/medium.ray:29 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).px_now, 0), "eq(elem(it.holes, 0).px_now, 0)");
});

test("tests/medium.ray:29 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).px_now, 0), "eq(elem(it.holes, 0).px_now, 0)");
});

test("tests/medium.ray:29 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).px_now, 0), "eq(elem(it.holes, 0).px_now, 0)");
});

test("tests/medium.ray:31 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.arrived(1, it.at(10, 7)), 0), "gt(it.arrived(1, it.at(10, 7)), 0)");
});

test("tests/medium.ray:31 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.arrived(1, it.at(10, 7)), 0), "gt(it.arrived(1, it.at(10, 7)), 0)");
});

test("tests/medium.ray:31 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.arrived(1, it.at(10, 7)), 0), "gt(it.arrived(1, it.at(10, 7)), 0)");
});

test("tests/medium.ray:32 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.rho.every(((v: any) => {
  return ge(v, it.rho_inf);
})), "it.rho.every(((v: any) => {\n  return ge(v, it.rho_inf);\n}))");
});

test("tests/medium.ray:32 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.rho.every(((v: any) => {
  return ge(v, it.rho_inf);
})), "it.rho.every(((v: any) => {\n  return ge(v, it.rho_inf);\n}))");
});

test("tests/medium.ray:32 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && eq(it.N, 16)) && eq(it.ticks, 12)))) { if (++guard > 14 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.rho.every(((v: any) => {
  return ge(v, it.rho_inf);
})), "it.rho.every(((v: any) => {\n  return ge(v, it.rho_inf);\n}))");
});

test("tests/medium.ray:40 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(elem(it.holes, 0).px_now, 0), "gt(elem(it.holes, 0).px_now, 0)");
});

test("tests/medium.ray:40 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(elem(it.holes, 0).px_now, 0), "gt(elem(it.holes, 0).px_now, 0)");
});

test("tests/medium.ray:40 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(elem(it.holes, 0).px_now, 0), "gt(elem(it.holes, 0).px_now, 0)");
});

test("tests/medium.ray:41 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 1).px_now, 0), "lt(elem(it.holes, 1).px_now, 0)");
});

test("tests/medium.ray:41 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 1).px_now, 0), "lt(elem(it.holes, 1).px_now, 0)");
});

test("tests/medium.ray:41 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 1).px_now, 0), "lt(elem(it.holes, 1).px_now, 0)");
});

test("tests/medium.ray:42 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(Math.abs(elem(it.holes, 0).py_now), 0.000001), "lt(Math.abs(elem(it.holes, 0).py_now), 0.000001)");
});

test("tests/medium.ray:42 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(Math.abs(elem(it.holes, 0).py_now), 0.000001), "lt(Math.abs(elem(it.holes, 0).py_now), 0.000001)");
});

test("tests/medium.ray:42 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(Math.abs(elem(it.holes, 0).py_now), 0.000001), "lt(Math.abs(elem(it.holes, 0).py_now), 0.000001)");
});

test("tests/medium.ray:43 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(elem(it.holes, 0).x, 6), "gt(elem(it.holes, 0).x, 6)");
});

test("tests/medium.ray:43 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(elem(it.holes, 0).x, 6), "gt(elem(it.holes, 0).x, 6)");
});

test("tests/medium.ray:43 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(elem(it.holes, 0).x, 6), "gt(elem(it.holes, 0).x, 6)");
});

test("tests/medium.ray:44 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 1).x, 42), "lt(elem(it.holes, 1).x, 42)");
});

test("tests/medium.ray:44 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 1).x, 42), "lt(elem(it.holes, 1).x, 42)");
});

test("tests/medium.ray:44 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 1).x, 42), "lt(elem(it.holes, 1).x, 42)");
});

test("tests/medium.ray:46 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(6, 24)), it.record_above(it.at(24, 24))), "gt(it.record_above(it.at(6, 24)), it.record_above(it.at(24, 24)))");
});

test("tests/medium.ray:46 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(6, 24)), it.record_above(it.at(24, 24))), "gt(it.record_above(it.at(6, 24)), it.record_above(it.at(24, 24)))");
});

test("tests/medium.ray:46 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(6, 24)), it.record_above(it.at(24, 24))), "gt(it.record_above(it.at(6, 24)), it.record_above(it.at(24, 24)))");
});

test("tests/medium.ray:47 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.crossed(it.at(24, 24)), 0), "gt(it.crossed(it.at(24, 24)), 0)");
});

test("tests/medium.ray:47 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.crossed(it.at(24, 24)), 0), "gt(it.crossed(it.at(24, 24)), 0)");
});

test("tests/medium.ray:47 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 2) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.crossed(it.at(24, 24)), 0), "gt(it.crossed(it.at(24, 24)), 0)");
});

test("tests/medium.ray:61 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(24, 18)), 0), "gt(it.record_above(it.at(24, 18)), 0)");
});

test("tests/medium.ray:61 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(24, 18)), 0), "gt(it.record_above(it.at(24, 18)), 0)");
});

test("tests/medium.ray:61 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(24, 18)), 0), "gt(it.record_above(it.at(24, 18)), 0)");
});

test("tests/medium.ray:62 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(30, 18)), 0), "gt(it.record_above(it.at(30, 18)), 0)");
});

test("tests/medium.ray:62 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(30, 18)), 0), "gt(it.record_above(it.at(30, 18)), 0)");
});

test("tests/medium.ray:62 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(30, 18)), 0), "gt(it.record_above(it.at(30, 18)), 0)");
});

test("tests/medium.ray:64 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(it.record_above(it.at(24, 18)), div(it.nf_inf, 100)), "lt(it.record_above(it.at(24, 18)), div(it.nf_inf, 100))");
});

test("tests/medium.ray:64 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(it.record_above(it.at(24, 18)), div(it.nf_inf, 100)), "lt(it.record_above(it.at(24, 18)), div(it.nf_inf, 100))");
});

test("tests/medium.ray:64 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(it.record_above(it.at(24, 18)), div(it.nf_inf, 100)), "lt(it.record_above(it.at(24, 18)), div(it.nf_inf, 100))");
});

test("tests/medium.ray:65 on alone_medium", (t) => {
  const it = G.medium(16, 8, 3, 18, 2).thrown(7, 7, 1, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(24, 18)), it.record_above(it.at(30, 18))), "gt(it.record_above(it.at(24, 18)), it.record_above(it.at(30, 18)))");
});

test("tests/medium.ray:65 on pair_medium", (t) => {
  const it = G.medium(49, 8, 3, 18, 3).thrown(6, 24, 1, 100, 0, 0, 1).thrown(42, 24, 1, 100, 0, 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(24, 18)), it.record_above(it.at(30, 18))), "gt(it.record_above(it.at(24, 18)), it.record_above(it.at(30, 18)))");
});

test("tests/medium.ray:65 on standing", (t) => {
  const it = G.medium(37, 8, 3, 18, 2).under_vacuum.thrown(18, 18, 0.01, 100, 0, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 1) && it.vacuum) && eq(it.ticks, 40)))) { if (++guard > 42 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.record_above(it.at(24, 18)), it.record_above(it.at(30, 18))), "gt(it.record_above(it.at(24, 18)), it.record_above(it.at(30, 18)))");
});

test("tests/motion.ray:13 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).px_now, 400), "ge(elem(it.holes, 0).px_now, 400)");
});

test("tests/motion.ray:13 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).px_now, 400), "ge(elem(it.holes, 0).px_now, 400)");
});

test("tests/motion.ray:13 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).px_now, 400), "ge(elem(it.holes, 0).px_now, 400)");
});

test("tests/motion.ray:13 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).px_now, 400), "ge(elem(it.holes, 0).px_now, 400)");
});

test("tests/motion.ray:14 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 0).px_now, 440), "lt(elem(it.holes, 0).px_now, 440)");
});

test("tests/motion.ray:14 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 0).px_now, 440), "lt(elem(it.holes, 0).px_now, 440)");
});

test("tests/motion.ray:14 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 0).px_now, 440), "lt(elem(it.holes, 0).px_now, 440)");
});

test("tests/motion.ray:14 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 0).px_now, 440), "lt(elem(it.holes, 0).px_now, 440)");
});

test("tests/motion.ray:15 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(Math.abs(elem(it.holes, 0).py_now), 1), "lt(Math.abs(elem(it.holes, 0).py_now), 1)");
});

test("tests/motion.ray:15 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(Math.abs(elem(it.holes, 0).py_now), 1), "lt(Math.abs(elem(it.holes, 0).py_now), 1)");
});

test("tests/motion.ray:15 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(Math.abs(elem(it.holes, 0).py_now), 1), "lt(Math.abs(elem(it.holes, 0).py_now), 1)");
});

test("tests/motion.ray:15 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(Math.abs(elem(it.holes, 0).py_now), 1), "lt(Math.abs(elem(it.holes, 0).py_now), 1)");
});

test("tests/motion.ray:16 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).y, 12), "eq(elem(it.holes, 0).y, 12)");
});

test("tests/motion.ray:16 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).y, 12), "eq(elem(it.holes, 0).y, 12)");
});

test("tests/motion.ray:16 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).y, 12), "eq(elem(it.holes, 0).y, 12)");
});

test("tests/motion.ray:16 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(elem(it.holes, 0).y, 12), "eq(elem(it.holes, 0).y, 12)");
});

test("tests/motion.ray:17 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).x, 15), "ge(elem(it.holes, 0).x, 15)");
});

test("tests/motion.ray:17 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).x, 15), "ge(elem(it.holes, 0).x, 15)");
});

test("tests/motion.ray:17 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).x, 15), "ge(elem(it.holes, 0).x, 15)");
});

test("tests/motion.ray:17 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!((eq(it.holes.length, 1) && eq(it.ticks, 20)))) { if (++guard > 22 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).x, 15), "ge(elem(it.holes, 0).x, 15)");
});

test("tests/motion.ray:25 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(elem(it.holes, 0).y, 18), "gt(elem(it.holes, 0).y, 18)");
});

test("tests/motion.ray:25 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(elem(it.holes, 0).y, 18), "gt(elem(it.holes, 0).y, 18)");
});

test("tests/motion.ray:25 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(elem(it.holes, 0).y, 18), "gt(elem(it.holes, 0).y, 18)");
});

test("tests/motion.ray:25 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(elem(it.holes, 0).y, 18), "gt(elem(it.holes, 0).y, 18)");
});

test("tests/motion.ray:26 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 1).y, 24), "lt(elem(it.holes, 1).y, 24)");
});

test("tests/motion.ray:26 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 1).y, 24), "lt(elem(it.holes, 1).y, 24)");
});

test("tests/motion.ray:26 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 1).y, 24), "lt(elem(it.holes, 1).y, 24)");
});

test("tests/motion.ray:26 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 1).y, 24), "lt(elem(it.holes, 1).y, 24)");
});

test("tests/motion.ray:27 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).px_now, 600), "ge(elem(it.holes, 0).px_now, 600)");
});

test("tests/motion.ray:27 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).px_now, 600), "ge(elem(it.holes, 0).px_now, 600)");
});

test("tests/motion.ray:27 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).px_now, 600), "ge(elem(it.holes, 0).px_now, 600)");
});

test("tests/motion.ray:27 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(elem(it.holes, 0).px_now, 600), "ge(elem(it.holes, 0).px_now, 600)");
});

test("tests/motion.ray:28 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 0).px_now, 660), "lt(elem(it.holes, 0).px_now, 660)");
});

test("tests/motion.ray:28 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 0).px_now, 660), "lt(elem(it.holes, 0).px_now, 660)");
});

test("tests/motion.ray:28 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 0).px_now, 660), "lt(elem(it.holes, 0).px_now, 660)");
});

test("tests/motion.ray:28 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 1)) && eq(it.ticks, 24)))) { if (++guard > 26 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(lt(elem(it.holes, 0).px_now, 660), "lt(elem(it.holes, 0).px_now, 660)");
});

test("tests/motion.ray:36 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 0.25)) && eq(it.ticks, 60)))) { if (++guard > 62 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, elem(it.holes, 0).x)), 2), Math.pow((sub(elem(it.holes, 1).y, elem(it.holes, 0).y)), 2)))), 18), "le(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, elem(it.holes, 0).x)), 2), Math.pow((sub(elem(it.holes, 1).y, elem(it.holes, 0).y)), 2)))), 18)");
});

test("tests/motion.ray:36 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 0.25)) && eq(it.ticks, 60)))) { if (++guard > 62 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, elem(it.holes, 0).x)), 2), Math.pow((sub(elem(it.holes, 1).y, elem(it.holes, 0).y)), 2)))), 18), "le(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, elem(it.holes, 0).x)), 2), Math.pow((sub(elem(it.holes, 1).y, elem(it.holes, 0).y)), 2)))), 18)");
});

test("tests/motion.ray:36 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 0.25)) && eq(it.ticks, 60)))) { if (++guard > 62 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, elem(it.holes, 0).x)), 2), Math.pow((sub(elem(it.holes, 1).y, elem(it.holes, 0).y)), 2)))), 18), "le(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, elem(it.holes, 0).x)), 2), Math.pow((sub(elem(it.holes, 1).y, elem(it.holes, 0).y)), 2)))), 18)");
});

test("tests/motion.ray:36 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 0.25)) && eq(it.ticks, 60)))) { if (++guard > 62 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, elem(it.holes, 0).x)), 2), Math.pow((sub(elem(it.holes, 1).y, elem(it.holes, 0).y)), 2)))), 18), "le(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, elem(it.holes, 0).x)), 2), Math.pow((sub(elem(it.holes, 1).y, elem(it.holes, 0).y)), 2)))), 18)");
});

test("tests/motion.ray:37 on line", (t) => {
  const it = G.field(6, 8, 1, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 0.25)) && eq(it.ticks, 60)))) { if (++guard > 62 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, 27)), 2), Math.pow((sub(elem(it.holes, 1).y, 18)), 2)))), 6), "ge(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, 27)), 2), Math.pow((sub(elem(it.holes, 1).y, 18)), 2)))), 6)");
});

test("tests/motion.ray:37 on alone", (t) => {
  const it = G.field(25, 8, 3, 1, 8, 2).thrown(6, 12, 1, 2000, 400, 0, 1);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 0.25)) && eq(it.ticks, 60)))) { if (++guard > 62 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, 27)), 2), Math.pow((sub(elem(it.holes, 1).y, 18)), 2)))), 6), "ge(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, 27)), 2), Math.pow((sub(elem(it.holes, 1).y, 18)), 2)))), 6)");
});

test("tests/motion.ray:37 on pair", (t) => {
  const it = G.field(43, 8, 3, 1, 8, 3).thrown(9, 18, 1, 2000, 600, 0, 1).thrown(33, 24, 1, 2000, (-600), 0, 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 0.25)) && eq(it.ticks, 60)))) { if (++guard > 62 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, 27)), 2), Math.pow((sub(elem(it.holes, 1).y, 18)), 2)))), 6), "ge(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, 27)), 2), Math.pow((sub(elem(it.holes, 1).y, 18)), 2)))), 6)");
});

test("tests/motion.ray:37 on orbit", (t) => {
  const it = G.field(37, 8, 3, 1, 8, 3).thrown(18, 18, 0.25, 4096, 0, 0, 1).thrown(27, 18, 1, 64, 0, (-19.2), 2);
  let guard = 0;
  while (!(((eq(it.holes.length, 2) && eq(elem(it.holes, 0).mx, 0.25)) && eq(it.ticks, 60)))) { if (++guard > 62 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(ge(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, 27)), 2), Math.pow((sub(elem(it.holes, 1).y, 18)), 2)))), 6), "ge(Math.sqrt((add(Math.pow((sub(elem(it.holes, 1).x, 27)), 2), Math.pow((sub(elem(it.holes, 1).y, 18)), 2)))), 6)");
});

test("tests/notation.ray:8 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(Notation.html(`\\bar{r}^{D-1}`), `<span class=\"bar\"><i>r</i></span><sup><b class=\"k\">D</b>-1</sup>`), "eq(Notation.html(`\\\\bar{r}^{D-1}`), `<span class=\\\"bar\\\"><i>r</i></span><sup><b class=\\\"k\\\">D</b>-1</sup>`)");
});

test("tests/notation.ray:9 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(Notation.parse(`\\frac{1}{2}`).length, 1), "eq(Notation.parse(`\\\\frac{1}{2}`).length, 1)");
});

test("tests/notation.ray:10 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(first(Notation.parse(`\\frac{1}{2}`)).kind, `frac`), "eq(first(Notation.parse(`\\\\frac{1}{2}`)).kind, `frac`)");
});

test("tests/notation.ray:11 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(Notation.html(`\\rho_{\\infty}`), `<i>ρ</i><sub>∞</sub>`), "eq(Notation.html(`\\\\rho_{\\\\infty}`), `<i>ρ</i><sub>∞</sub>`)");
});

test("tests/notation.ray:12 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(Notation.html(`\\text{the } \\rho`), `<span class=\"tx\">the </span> <i>ρ</i>`), "eq(Notation.html(`\\\\text{the } \\\\rho`), `<span class=\\\"tx\\\">the </span> <i>ρ</i>`)");
});

test("tests/notation.ray:13 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(Notation.html(`DEG`), `<span class=\"bar\"><b class=\"k\">DEG</b></span>`), "eq(Notation.html(`DEG`), `<span class=\\\"bar\\\"><b class=\\\"k\\\">DEG</b></span>`)");
});

test("tests/notation.ray:14 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(Notation.html(`l.shell(R)`), `<span class=\"mu\">l.</span><b class=\"k\">shell</b>(<span class=\"bar\">R</span>)`), "eq(Notation.html(`l.shell(R)`), `<span class=\\\"mu\\\">l.</span><b class=\\\"k\\\">shell</b>(<span class=\\\"bar\\\">R</span>)`)");
});

test("tests/notation.ray:16 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(Notation.html(`\\sum_{r}^{R} x`), `<span class=\"big\"><span class=\"sign\">&#8721;</span><span class=\"lim\"><sup><span class=\"bar\">R</span></sup><sub><i>r</i></sub></span></span> <span class=\"mu\">x</span>`), "eq(Notation.html(`\\\\sum_{r}^{R} x`), `<span class=\\\"big\\\"><span class=\\\"sign\\\">&#8721;</span><span class=\\\"lim\\\"><sup><span class=\\\"bar\\\">R</span></sup><sub><i>r</i></sub></span></span> <span class=\\\"mu\\\">x</span>`)");
});

test("tests/notation.ray:17 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(Notation.html(`\\sum_{r}^{R} y`), `<span class=\"big\"><span class=\"sign\">&#8721;</span><span class=\"lim\"><sup><span class=\"bar\">R</span></sup><sub><i>r</i></sub></span></span> <i>y</i>`), "eq(Notation.html(`\\\\sum_{r}^{R} y`), `<span class=\\\"big\\\"><span class=\\\"sign\\\">&#8721;</span><span class=\\\"lim\\\"><sup><span class=\\\"bar\\\">R</span></sup><sub><i>r</i></sub></span></span> <i>y</i>`)");
});

test("tests/notation.ray:18 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(Notation.html(`[[ehrhart]]`).startsWith(`<a class=\"ref\"`), "Notation.html(`[[ehrhart]]`).startsWith(`<a class=\\\"ref\\\"`)");
});

test("tests/notation.ray:19 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(!eq(Notation.banned(`a − b`), null), "!eq(Notation.banned(`a − b`), null)");
});

test("tests/notation.ray:20 on rbar", (t) => {
  const it = first(Notation.parse(`\\bar{r}^{D-1}`));
  let guard = 0;
  while (!(eq(it.kind, `scripted`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(Notation.banned(`a - b`), null), "eq(Notation.banned(`a - b`), null)");
});

test("tests/reading.ray:9 on read", (t) => {
  const it = G;
  let guard = 0;
  while (!(eq(it.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.equation.latex, `\\partial_{t} n + \\hat{d} \\cdot \\nabla_{l} n + \\paren{\\nabla n_{f}} \\cdot \\nabla_{\\hat{d}} n = - 2 \\sigma F n^{2} + \\bar{DEG} \\nu \\paren{1 - \\rho} + l.\\bar{m} \\omega`), "eq(it.equation.latex, `\\\\partial_{t} n + \\\\hat{d} \\\\cdot \\\\nabla_{l} n + \\\\paren{\\\\nabla n_{f}} \\\\cdot \\\\nabla_{\\\\hat{d}} n = - 2 \\\\sigma F n^{2} + \\\\bar{DEG} \\\\nu \\\\paren{1 - \\\\rho} + l.\\\\bar{m} \\\\omega`)");
});

test("tests/reading.ray:11 on read", (t) => {
  const it = G;
  let guard = 0;
  while (!(eq(it.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.equation.terms.some(((t: any) => {
  return ((((!eq(t.rule, null) && eq(t.rule.id, `/1`)) && eq(t.doing.rays.source, `-2`)) && eq(t.doing.space.source, `-1`)) && eq(t.doing.folds.source, `1`));
})), "it.equation.terms.some(((t: any) => {\n  return ((((!eq(t.rule, null) && eq(t.rule.id, `/1`)) && eq(t.doing.rays.source, `-2`)) && eq(t.doing.space.source, `-1`)) && eq(t.doing.folds.source, `1`));\n}))");
});

test("tests/reading.ray:13 on read", (t) => {
  const it = G;
  let guard = 0;
  while (!(eq(it.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.equation.terms.some(((t: any) => {
  return ((((!eq(t.rule, null) && eq(t.rule.id, `/1`)) && eq(t.doing.share.source, `(1 - s.ρ) * s.F`)) && eq(t.doing.space.source, `1`)) && eq(t.doing.grew.source, `1`));
})), "it.equation.terms.some(((t: any) => {\n  return ((((!eq(t.rule, null) && eq(t.rule.id, `/1`)) && eq(t.doing.share.source, `(1 - s.ρ) * s.F`)) && eq(t.doing.space.source, `1`)) && eq(t.doing.grew.source, `1`));\n}))");
});

test("tests/reading.ray:15 on read", (t) => {
  const it = G;
  let guard = 0;
  while (!(eq(it.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.equation.terms.some(((t: any) => {
  return (((!eq(t.rule, null) && eq(t.rule.id, `/2`)) && eq(t.doing.rays.source, `s.DEG`)) && eq(t.doing.share.source, `1 - s.ρ`));
})), "it.equation.terms.some(((t: any) => {\n  return (((!eq(t.rule, null) && eq(t.rule.id, `/2`)) && eq(t.doing.rays.source, `s.DEG`)) && eq(t.doing.share.source, `1 - s.ρ`));\n}))");
});

test("tests/reading.ray:17 on read", (t) => {
  const it = G;
  let guard = 0;
  while (!(eq(it.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.equation.terms.some(((t: any) => {
  return (((!eq(t.rule, null) && eq(t.rule.id, `/2`)) && eq(t.doing.folds.source, `-s.DEG`)) && eq(t.doing.share.source, `s.n_f / s.DEG * (1 - s.ρ)`));
})), "it.equation.terms.some(((t: any) => {\n  return (((!eq(t.rule, null) && eq(t.rule.id, `/2`)) && eq(t.doing.folds.source, `-s.DEG`)) && eq(t.doing.share.source, `s.n_f / s.DEG * (1 - s.ρ)`));\n}))");
});

test("tests/reading.ray:18 on read", (t) => {
  const it = G;
  let guard = 0;
  while (!(eq(it.name, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.equation.terms.some(((t: any) => {
  return (((!eq(t.rule, null) && eq(t.rule.id, `/2`)) && eq(t.doing.space.source, `1`)) && eq(t.doing.share.source, `s.n_f * (1 - s.ρ)`));
})), "it.equation.terms.some(((t: any) => {\n  return (((!eq(t.rule, null) && eq(t.rule.id, `/2`)) && eq(t.doing.space.source, `1`)) && eq(t.doing.share.source, `s.n_f * (1 - s.ρ)`));\n}))");
});

test("tests/theorems.ray:8 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.has(`vacuum.equation`), "it.has(`vacuum.equation`)");
});

test("tests/theorems.ray:9 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.standing(`vacuum.occupancy`), "it.standing(`vacuum.occupancy`)");
});

test("tests/theorems.ray:10 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.standing(`force.range`), "it.standing(`force.range`)");
});

test("tests/theorems.ray:11 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.concluded(`vacuum.facing`), `F = \\frac{1}{2}`), "eq(it.concluded(`vacuum.facing`), `F = \\\\frac{1}{2}`)");
});

test("tests/theorems.ray:12 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.concluded(`force.range`), `\\lambda = \\frac{2}{\\rho}`), "eq(it.concluded(`force.range`), `\\\\lambda = \\\\frac{2}{\\\\rho}`)");
});

test("tests/theorems.ray:13 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.standing(`gravity.falloff`), "it.standing(`gravity.falloff`)");
});

test("tests/theorems.ray:14 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.standing(`gravity.horizon`), "it.standing(`gravity.horizon`)");
});

test("tests/theorems.ray:15 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.ids.length, 51), "eq(it.ids.length, 51)");
});

test("tests/theorems.ray:16 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.standing(`gravity.coincidence`), "it.standing(`gravity.coincidence`)");
});

test("tests/theorems.ray:17 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.standing(`gravity.bent`), "it.standing(`gravity.bent`)");
});

test("tests/theorems.ray:18 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.standing(`gravity.standing`), "it.standing(`gravity.standing`)");
});

test("tests/theorems.ray:19 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.standing(`gravity.mass`), "it.standing(`gravity.mass`)");
});

test("tests/theorems.ray:20 on closure", (t) => {
  const it = G.proved;
  let guard = 0;
  while (!(eq(it.theory, `G`))) { if (++guard > 8 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.standing(`gravity.newton`), "it.standing(`gravity.newton`)");
});

test("tests/vacuum.ray:16 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.rays, 0), "eq(it.rays, 0)");
});

test("tests/vacuum.ray:16 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.rays, 0), "eq(it.rays, 0)");
});

test("tests/vacuum.ray:16 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.rays, 0), "eq(it.rays, 0)");
});

test("tests/vacuum.ray:16 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.rays, 0), "eq(it.rays, 0)");
});

test("tests/vacuum.ray:17 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.points, it.vertices.length), "eq(it.points, it.vertices.length)");
});

test("tests/vacuum.ray:17 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.points, it.vertices.length), "eq(it.points, it.vertices.length)");
});

test("tests/vacuum.ray:17 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.points, it.vertices.length), "eq(it.points, it.vertices.length)");
});

test("tests/vacuum.ray:17 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.points, it.vertices.length), "eq(it.points, it.vertices.length)");
});

test("tests/vacuum.ray:18 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.annihilations, 0), "eq(it.annihilations, 0)");
});

test("tests/vacuum.ray:18 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.annihilations, 0), "eq(it.annihilations, 0)");
});

test("tests/vacuum.ray:18 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.annihilations, 0), "eq(it.annihilations, 0)");
});

test("tests/vacuum.ray:18 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(eq(it.ticks, 0))) { if (++guard > 2 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.annihilations, 0), "eq(it.annihilations, 0)");
});

test("tests/vacuum.ray:23 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.created, Math.pow(it.N, it.geometry.D)), "eq(it.created, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:23 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.created, Math.pow(it.N, it.geometry.D)), "eq(it.created, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:23 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.created, Math.pow(it.N, it.geometry.D)), "eq(it.created, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:23 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.created, Math.pow(it.N, it.geometry.D)), "eq(it.created, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:24 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.rays, 0), "gt(it.rays, 0)");
});

test("tests/vacuum.ray:24 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.rays, 0), "gt(it.rays, 0)");
});

test("tests/vacuum.ray:24 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.rays, 0), "gt(it.rays, 0)");
});

test("tests/vacuum.ray:24 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.rays, 0), "gt(it.rays, 0)");
});

test("tests/vacuum.ray:25 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.points, Math.pow(it.N, it.geometry.D)), "eq(it.points, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:25 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.points, Math.pow(it.N, it.geometry.D)), "eq(it.points, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:25 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.points, Math.pow(it.N, it.geometry.D)), "eq(it.points, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:25 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 1) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 3 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(eq(it.points, Math.pow(it.N, it.geometry.D)), "eq(it.points, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:31 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!((eq(it.ticks, 3) && gt(it.margin, 0)))) { if (++guard > 5 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.points, Math.pow(it.N, it.geometry.D)), "gt(it.points, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:31 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!((eq(it.ticks, 3) && gt(it.margin, 0)))) { if (++guard > 5 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.points, Math.pow(it.N, it.geometry.D)), "gt(it.points, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:31 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!((eq(it.ticks, 3) && gt(it.margin, 0)))) { if (++guard > 5 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.points, Math.pow(it.N, it.geometry.D)), "gt(it.points, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:31 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!((eq(it.ticks, 3) && gt(it.margin, 0)))) { if (++guard > 5 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.points, Math.pow(it.N, it.geometry.D)), "gt(it.points, Math.pow(it.N, it.geometry.D))");
});

test("tests/vacuum.ray:32 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!((eq(it.ticks, 3) && gt(it.margin, 0)))) { if (++guard > 5 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.vertices.every(((v: any) => {
  return v.at.components.every(((c: any) => {
    return (ge(c, sub(0, it.margin)) && lt(c, add(it.N, it.margin)));
  }));
})), "it.vertices.every(((v: any) => {\n  return v.at.components.every(((c: any) => {\n    return (ge(c, sub(0, it.margin)) && lt(c, add(it.N, it.margin)));\n  }));\n}))");
});

test("tests/vacuum.ray:32 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!((eq(it.ticks, 3) && gt(it.margin, 0)))) { if (++guard > 5 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.vertices.every(((v: any) => {
  return v.at.components.every(((c: any) => {
    return (ge(c, sub(0, it.margin)) && lt(c, add(it.N, it.margin)));
  }));
})), "it.vertices.every(((v: any) => {\n  return v.at.components.every(((c: any) => {\n    return (ge(c, sub(0, it.margin)) && lt(c, add(it.N, it.margin)));\n  }));\n}))");
});

test("tests/vacuum.ray:32 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!((eq(it.ticks, 3) && gt(it.margin, 0)))) { if (++guard > 5 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.vertices.every(((v: any) => {
  return v.at.components.every(((c: any) => {
    return (ge(c, sub(0, it.margin)) && lt(c, add(it.N, it.margin)));
  }));
})), "it.vertices.every(((v: any) => {\n  return v.at.components.every(((c: any) => {\n    return (ge(c, sub(0, it.margin)) && lt(c, add(it.N, it.margin)));\n  }));\n}))");
});

test("tests/vacuum.ray:32 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!((eq(it.ticks, 3) && gt(it.margin, 0)))) { if (++guard > 5 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.vertices.every(((v: any) => {
  return v.at.components.every(((c: any) => {
    return (ge(c, sub(0, it.margin)) && lt(c, add(it.N, it.margin)));
  }));
})), "it.vertices.every(((v: any) => {\n  return v.at.components.every(((c: any) => {\n    return (ge(c, sub(0, it.margin)) && lt(c, add(it.N, it.margin)));\n  }));\n}))");
});

test("tests/vacuum.ray:37 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.created, 0), "gt(it.created, 0)");
});

test("tests/vacuum.ray:37 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.created, 0), "gt(it.created, 0)");
});

test("tests/vacuum.ray:37 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.created, 0), "gt(it.created, 0)");
});

test("tests/vacuum.ray:37 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.created, 0), "gt(it.created, 0)");
});

test("tests/vacuum.ray:38 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.annihilations, 0), "gt(it.annihilations, 0)");
});

test("tests/vacuum.ray:38 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.annihilations, 0), "gt(it.annihilations, 0)");
});

test("tests/vacuum.ray:38 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.annihilations, 0), "gt(it.annihilations, 0)");
});

test("tests/vacuum.ray:38 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.annihilations, 0), "gt(it.annihilations, 0)");
});

test("tests/vacuum.ray:39 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.folded, 0), "gt(it.folded, 0)");
});

test("tests/vacuum.ray:39 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.folded, 0), "gt(it.folded, 0)");
});

test("tests/vacuum.ray:39 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.folded, 0), "gt(it.folded, 0)");
});

test("tests/vacuum.ray:39 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(gt(it.folded, 0), "gt(it.folded, 0)");
});

test("tests/vacuum.ray:40 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(it.folded, it.annihilations), "le(it.folded, it.annihilations)");
});

test("tests/vacuum.ray:40 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(it.folded, it.annihilations), "le(it.folded, it.annihilations)");
});

test("tests/vacuum.ray:40 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(it.folded, it.annihilations), "le(it.folded, it.annihilations)");
});

test("tests/vacuum.ray:40 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(it.folded, it.annihilations), "le(it.folded, it.annihilations)");
});

test("tests/vacuum.ray:42 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.vertices.every(((v: any) => {
  return ge(v.density, 1);
})), "it.vertices.every(((v: any) => {\n  return ge(v.density, 1);\n}))");
});

test("tests/vacuum.ray:42 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.vertices.every(((v: any) => {
  return ge(v.density, 1);
})), "it.vertices.every(((v: any) => {\n  return ge(v.density, 1);\n}))");
});

test("tests/vacuum.ray:42 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.vertices.every(((v: any) => {
  return ge(v.density, 1);
})), "it.vertices.every(((v: any) => {\n  return ge(v.density, 1);\n}))");
});

test("tests/vacuum.ray:42 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(it.vertices.every(((v: any) => {
  return ge(v.density, 1);
})), "it.vertices.every(((v: any) => {\n  return ge(v.density, 1);\n}))");
});

test("tests/vacuum.ray:44 on square", (t) => {
  const it = G.seed(SQUARE4, 4, 1);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(it.points, it.vertices.length), "le(it.points, it.vertices.length)");
});

test("tests/vacuum.ray:44 on diagonal", (t) => {
  const it = G.seed(SQUARE8, 5, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(it.points, it.vertices.length), "le(it.points, it.vertices.length)");
});

test("tests/vacuum.ray:44 on cubic", (t) => {
  const it = G.seed(CUBIC6, 4, 3);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(it.points, it.vertices.length), "le(it.points, it.vertices.length)");
});

test("tests/vacuum.ray:44 on frontier", (t) => {
  const it = G.seed(SQUARE4, 3, 5, 250000, 2);
  let guard = 0;
  while (!(((eq(it.ticks, 4) && (it.sources.length === 0)) && eq(it.margin, 0)))) { if (++guard > 6 || !("tick" in it)) return t.skip("the refinement never held"); it.tick; }
  assert.ok(le(it.points, it.vertices.length), "le(it.points, it.vertices.length)");
});
